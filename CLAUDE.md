# CLAUDE.md — Lixus AI proje hafızası

> Her oturum başında otomatik okunur. Kritik bağlam, kurallar, "unutulmayacaklar".
> Plan → `ROADMAP.md`. Ayrıntılı geçmiş → git log. (2026-07-13'te sadeleştirildi;
> eski uzun oturum-anlatıları git geçmişinde durur.)

## Ürün
**Lixus AI** — Türkiye odaklı, çok kiracılı (multi-tenant) SaaS. Kısa dönem kiralama
hostları için Airbnb/Booking misafir mesajlarını AI ile yanıtlar. B2C; bireysel Türk
hostlara satılır. Sahip/operatör: musaxd57 (Nuve, ~10 daire). **Türkçe öncelikli.**
Marka: **Lixus AI** (site: lixusai.com). Temel ilke: AI karar-verici değil, yardımcı
operatör — riskli mesajlar hep insana bırakılır.

## 🚨 Değişmez kural
**Çalışan ürün BOZULMAYACAK.** Her ekleme *additive*, testli, geri alınabilir. Riskli
adımda önce yedek + kullanıcı onayı. Para/e-posta akışına dokunan şeyler kullanıcı
onayıyla ve ilk denemeler birlikte doğrulanarak açılır. Build + `npm test` yeşil
olmadan push etme. PR sadece kullanıcı isterse.

## ⚠️ DOKUNMA / DİKKAT (asla kaybetme)
- **`senderName: "GuestOps AI"`** (automation.ts, reports.ts, sent/page.tsx) = mesaj-
  sınıflandırma SİHİRLİ STRING'i (AI mesajını ayırır). **DEĞİŞTİRME** — eski/yeni DB
  satırları bölünür. Görünür marka "Lixus AI"e rebrand edildi ama bu string aynı kaldı
  (`displaySenderName()` sadece render'da map'ler). Rapor sayımı `senderName OR aiAssisted`.
- **Konteyner reset:** ortam ara sıra eski snapshot'a (365c957) döner; dosyalar eksik/
  tuhaf görünürse PANİK YOK. Kurtarma: `git fetch origin claude/great-edison-3zqpZ` →
  `git reset --hard origin/claude/great-edison-3zqpZ` → `npx prisma generate`. İş hep
  origin'de güvende → **SIK COMMIT+PUSH şart** (edit→hemen commit, pencere açık bırakma).
- **Migration ZORUNLU:** boot artık `prisma migrate deploy` (db push DEĞİL). `schema.prisma`'yı
  elle değiştirince MUTLAKA gerçek migration üret (`prisma migrate diff --from-migrations
  ./prisma/migrations --to-schema-datamodel ./prisma/schema.prisma --script`), taze throwaway
  Postgres'te sıfır-drift doğrula. Yoksa şema↔DB sessizce ayrışır, deploy'da patlar.
- **Dolu tabloya ASLA `@unique`/required-no-default/drop ekleme** — boot'ta patlar
  (chatToken outage dersi). App-level benzersizlik + findFirst kullan. Index + yeni tablo güvenli.
- **Git:** push author "Unverified" olmasın → `user.email noreply@anthropic.com`, `user.name Claude`.
  Tag push proxy'de çalışmaz → yedek TAG değil BRANCH (`backup/stable-YYYY-MM-DD`).
  Commit mesajında backtick KULLANMA (bash çalıştırıp kelime düşürür) → heredoc kullan.
- **ENCRYPTION_KEY rotasyonu = ASLA** (canlı token'lar kırılır).
- **APP_URL sabit taban (host-injection fix):** e-posta doğrulama linki + OAuth redirect_uri artık `appBaseUrl()` (sabit `APP_URL` env / canonical) kullanır, ASLA request Host'u değil (forged Host → token saldırgan alanına gidiyordu). In-browser redirect'ler (logout/verify/oauth) `baseUrlFromHost` ama artık ALLOWLIST'li (localhost/127/canonical/APP_URL host; gerisi canonical'a düşer). Railway'e `APP_URL` ekle.
- **Railway arkasında hiçbir route `req.url`'den mutlak URL kurmasın** → `baseUrlFromHost(
  req.headers.get("host"))` (localhost redirect bug dersi).

## Teknik mimari
- **Stack:** Next.js 15 (App Router) · Prisma 6.2 · PostgreSQL · Railway. Boot (Dockerfile):
  `prisma migrate deploy && npm run start`. Branch `claude/great-edison-3zqpZ` → Railway oto-deploy.
- **Multi-tenancy:** `Organization`. Operatör (super-admin, `SUPERADMIN_EMAILS`) impersonation
  ile müşteri org'una girer (JWT `actorUserId`/`actorEmail`). ~58 route org-scoped, IDOR yok.
- **Auth:** JWT + `sessionEpoch` (çalınan token şifre-değişince/reset'te düşer — server-enforced).
  `withAuth`/`withManage` HOF'ları (lib/route-guard.ts, ayrı modül — cross-module mock için).
  2FA TOTP (replay-korumalı). Şifre değiştirme 2-adım e-posta kodu (mevcut şifre sorulmaz).
- **Hospitable:** per-tenant şifreli token (`getOrgHospitableToken` = sync/gönderimin TEK
  fonksiyonu). PAT (süresiz) VEYA OAuth (12s access + 90g rotasyonlu refresh, şeffaf yenileme).
  `financials:read` YOK → gelir özelliği bilinçli KALDIRILDI, geri ekleme. Env-token fallback
  SADECE primary org'a (`PRIMARY_ORG_ID`=Nuve `cmpwcnpdz0000oz1yw4wof2o1`).
- **Sync:** cross-instance kilit (`SystemLock` "scheduled-sync") + 2-dk in-process cron
  (instrumentation.ts, `INTERNAL_CRON_DISABLED` ile kapanır) + opsiyonel dış cron
  (`/api/cron/sync`, `CRON_SECRET` header). Hepsi `runScheduledSync`. Manuel sync `withSyncLock`.
- **Gönderim tek-nokta:** `sendOnChannel` (messaging.ts) = ikinci PMS adaptörü buraya takılır.
- **Model:** OpenAI `gpt-5.1` (ucuz, prompt-cache aktif). Değiştirme = gönderim hot-path'i
  yeniden kalibrasyon riski; sadece gerçek arıza + A/B ile değiştir.

## AI GÜVENLİK MİMARİSİ (ürünün kalbi)
**Tek kaynak:** `src/lib/ai/prompts.ts` (sistem prompt + 20 örnek) + `fallback.ts` (OpenAI'sız
deterministik yol + kelime ağları). TÜM yüzeyler buradan: oto-yanıt, inbox AI-öner, Ayarlar
"AI'yı Deneyin", landing demo, QR concierge. Demoya özel hiçbir şey yok.
**3-SEVİYE (kullanıcı modeli):**
- **Seviye-1 düşük risk → OTO-GÖNDER:** wifi/giriş/otopark/çöp/checkout gibi KB'den net konular.
- **Seviye-2 orta risk → opt-in "holding ack":** hafif şikayet (para/güvenlik/tehdit YOK). Host
  `Organization.autoHoldingReplyEnabled` (default KAPALI) açarsa tek deterministik bekletme mesajı
  gider (6 dil; söz vermez, suç kabul etmez, foto/detay ister, host'a devreder). Konuşma YİNE
  "Sorunlu" + e-posta. Açılmazsa landing sözü ("şikayeti otomatik yanıtlamaz") aynen doğru.
- **Seviye-3 yüksek risk → SESSİZ TASLAK + host'a acil mail + "Sorunlu":** şikayet/iade/iptal/
  kötü-yorum tehdidi/platform-dışı ödeme/insan-talebi/güvenlik-acili/injection/ayrımcılık.
**KOD KAPISI (`passesAutoReplySafetyGate`, karar MODELE verilmez):** source==openai + intent
BLOCKLIST + kelime-ağı çapraz-kontrolü + deterministik injection vetosu + bilinmeyen-intent clamp
(→general, conf≤0.5) + yüksek-riskli riskType vetosu + confidence≥0.75. TEK muafiyet: intent VE
riskType birlikte human_request (tasarlanmış devir mesajı + AI 12s susar).
**riskType/evidence (Faz-B):** model çıktısı `riskType` (11'lik kapalı set, kod-clamp), `usedSources`
(kanıt, KODDA doğrulanır — uydurma kaynak düşer), `missingInfo`. UI: öneri paneli/test kartı "İnsan
incelemesi: X" + "Kullandığı bağlam" + "Eksik bilgi"; inbox amber rozet; Raporlar "AI Risk Görünümü
(30g)" kartı. Persist: `Conversation.{skippedReason,lastRiskLevel,lastRiskType}` + `Message.aiSourcesJson`.
**GOLDEN SET** (`tests/unit/golden-scenarios.test.ts`): ~105 sabit senaryo, kapıya bilerek zararsız
0.9-güven verilir → "model yanlış sınıflandırsa bile kod vetolar mı" asserte edilir. **KURAL: prompt/
kelime-ağı değişikliği yapan HERKES golden seti çalıştırır; yeni risk sınıfına hem tehdit hem övgü-
tuzağı senaryosu ekler.** **3 YENİ desen (araştırma turu 07-16, web-doğrulanmış):** öz-zarar/ruh
sağlığı krizi (→safety_emergency, deterministik `SAFETY_CRITICAL_WORDS`'e katlandı, bot asla oto-yanıt
vermez, şefkatli+acil-yardıma yönlendiren taslak+insana devir) · squatting/çıkışı-reddetme (→rule_violation,
yeni `OVERSTAY_REFUSAL_PHRASES` net, red-çapalı; NORMAL uzatma talebi squatting DEĞİL) · kodlanmış/obfuske
injection (base64/ters-metin, INJECTION_PATTERNS'e yüksek-hassasiyet marker). Deterministik yol OpenAI'sız
da vetolar; #1 tıbbi-acil zaten kapsanıyordu. Fallback secret-gate: wifi/checkin/adres KB içeriği SADECE confirmed/
completed konaklamada (QR `verifiedActiveStay` flag'i ile). Üslup kuralları: duygu beyanı yasak,
temenni yasak, çelişki yasak, dolgu-soru yasak, misafire HER ZAMAN "siz", ben-dili (nezaket çoğul).

## MEVCUT CANLI DURUM (env'ler Railway'de AÇIK)
- **`AUTO_REPLY_ENABLED=1`** ✅ (kullanıcı açtı) → otomatik mesaj artık gerçekten gidebilir (org
  toggle + aktif-saat + güvenlik kapısından geçen). ⚠️ İlk gerçek gönderimleri Gönderilenler'den doğrula.
- **`BILLING_ENFORCED=true`** ✅ → 14g deneme bitince TAM KİLİT YOK; org "ücretsiz sürüme" düşer
  (gezer/manuel çalışır) ama oto-mesajlaşma kapanır (`premiumAllowed` tek kapı; grandfathered/
  superadmin=founder hiç etkilenmez). `LimitedModeBanner`. past_due 14g grace (Paddle dunning).
- **Paddle PRODUCTION CANLI** (env=production, canlı key, webhook `www.lixusai.com/api/webhooks/paddle`
  aktif). Sandbox uçtan-uca doğrulandı. ✅ **GERÇEK ödeme CANLIDA doğrulandı** (Başlangıç paketi gerçek
  kartla satın alındı → ardından in-app upgrade ile Pro'ya yükseltildi; Paddle vendor dashboard kanıtı:
  MRR $15.98, 1 aktif abone, Revenue $16.44, 2 ödemeli işlem). Upgrade akışı da bu testle CANLI doğrulandı.
- **`DATA_RETENTION_MONTHS=24`** ✅ (24 aydan eski misafir PII anonimleşir), **`TRIAL_EMAILS_ENABLED=1`** ✅,
  **`LANDING_DEMO_ENABLED=1`** ✅ (kayıtsız AI demo; IP 6/saat + günlük `LANDING_DEMO_DAILY_CAP` vars.300),
  **`REGISTRATION_OPEN=1`** ✅, **`GUEST_CHAT_ENABLED`** (QR concierge kill-switch).
- **Hospitable OAuth** (`HOSPITABLE_OAUTH_CLIENT_ID/SECRET`) canlı → "Hospitable ile Bağlan" butonu
  çalışıyor (uçtan-uca gerçek hesapla doğrulandı). **`TRUST_CF_HEADER` EKLEME** (origin CF arkasında
  değil, Paddle webhook direkt gelir → default=rightmost XFF doğru).
- **Nuve'nin Hospitable aboneliği bitik (402)** → veri donmuş anlık-görüntü; yenilenince canlı döner (bug değil).

## FİYAT (kesin, 2026-06-16 kullanıcı onaylı) — FLAT tier'ler
**Başlangıç ₺449 (1-2 daire) · Pro ₺899 (3-7) · İşletme ₺1.699 (8-25 daire)**; yıllıkta 2 ay bedava.
Reverse-trial: kayıt → 14g tam Pro ücretsiz (KART YOK, e-posta doğrulama şart), yükseltmezse ücretsiz
sürüme düşer. Flat KALDI (dinamik/mülk-başı REDDEDİLDİ — AI maliyeti ~₺17/daire, marj %75-96; flat-rate
bias + Türk pazarı). İşletme `propertyLimit=25` (canAddProperty + sync-limiti buna bağlı). "25+ → bize ulaşın".
USD/TRY=₺46. **legal-entity.ts SELLER alanları DOLDURULDU** ✅ (unvan+adres DPA'dan, P.IVA+telefon
gerçek değerlerle 07-18'de tamamlandı; Paddle MoR satıcı gösterimi hâlâ avukat sorusu).

## 🎯 HOSPITABLE ORTAKLIĞI (kullanıcı direktifi — ASLA UNUTMA)
**#1 SORU:** Hospitable, Lixus müşterilerine özel **"sadece-API / limited" paketi** çıkarır mı? Müşteri
Hospitable panelini kullanmadan, yalnızca Lixus'un çektiği veriyi (property/reservation/message read +
**message:write**) alsın. Amaç: bugünkü **$29/ay Hospitable-app maliyet engelini kaldırmak** (asıl müşteri-
edinme engeli). Fiyat hipotezi ~$7/daire (placeholder). Sıra: ÖNCE fizibilite SONRA fiyat. Pitch: "Lixus
= Hospitable'ı Türkiye'de büyüten dağıtım kanalı" (kazan-kazan).
**DOĞRULANMIŞ (web, 2026-07-16 — help.hospitable.com):** Public API/PAT erişimi ÜCRETLİ planlarda (Host/
Professional/Mogul); **ücretsiz Essentials'ta API YOK + Connected Integrations YOK** → Lixus'a mesaj/rezervasyon
çekilemez. ⚠️ Essentials'ın connect'te 402 mi 403 mü döndüğü CANLI payload olmadan KESİN DEĞİL → connect route
İKİSİNİ AYRI map'ler: 402=abonelik API'yi kapsamıyor (plana yönlendir), 403=plan VEYA token yetkileri yetersiz
(token'ın doğru olduğunu ASLA iddia etme — 403'te token yetkisi eksik olabilir). Hospitable **partnerlerden PAT
yerine OAuth vendor flow BEKLİYOR** (bizde "Hospitable ile Bağlan" = OAuth ana yol; PAT fallback). PAT yolu:
my.hospitable.com → **Apps** (veya Settings→Integrations) →
**API access** → **Access tokens** sekmesi → "+ Add new" (canlı UI + web doğrulandı; ESKİ "Settings→API"
YANLIŞ, "API access" ara adımı EKSİKTİ — düzeltildi); PAT 1 yıl geçerli, yalnız owner/admin üretir, **Read+Write**
izni gerekir (mesaj gönderimi için write), kopyalarken Hospitable şifresi sorulur. Fiyatlar: Host $25.52/
Professional $51.92/Mogul $87.12 (yıllık aylık) + ücretsiz Essentials. **OAuth 500 gözlemi (2026-07-16, ekran
görüntüsü):** "Hospitable ile Bağlan" → auth.hospitable.com sayfası YÜKLENDİ ama Hospitable'ın KENDİ backend'i
(`/api/oauth/authorize`) 500 döndü = HATA BİZDE DEĞİL (bizim rota yalnız doğru authorize URL'ye redirect eder +
state cookie; sayfa açıldı demek redirect_uri kabul edildi). En güçlü hipotez: yetkilendirilen Hospitable hesabının
AKTİF ÜCRETLİ aboneliği yok (Nuve'ninki 402'ye düştü / Essentials) → Hospitable entegrasyonu veremeyip 500'lüyor
(kesin teyit Hospitable log'u ister). Connect kartındaki amber plan-uyarısı artık OAuth butonunun DA ÜSTÜNDE.
**Durum (Patrick, 2026-07-02):** Connect KAPANDI (tam mesajlaşma yok, sadece Airbnb-bağlama). **Public API
+ OAuth vendor flow** tek yol (host'un ÜCRETLİ Hospitable aboneliği ŞART). White-label/reseller ŞU AN YOK ("trafik
gösterince pilot konuş" — kapı açık). **YENİ FIRSAT:** Patrick "tek ana hesap altında birden fazla host mülkü
yönetmek mümkün, bariyeri düşürebilir, white-label'lanabilir" dedi → property-manager modeli. ⚠️ Mimari not
(kullanıcı onayı gerek, kod DEĞİL): "tek hesap→çoklu tenant" bugünkü per-org token modelinden farklı, tenant-
izolasyonu yeniden tasarlanmalı. Partner Portal: partners.hospitable.com. Sessizlik normal, 5 iş günü dürtme yok.

## MEVCUT ÖZELLİKLER (var, tekrar ekleme)
Panel: dashboard (AI özet, 6-adım onboarding sağlık kontrolü), inbox (AI-öner + risk rozeti), Mesajlar,
Misafir Sohbetleri (QR concierge, kapalı-doğumlu), Gönderilenler, Görevler (Kanban), **Takvim** (/calendar,
aylık grid), İptaller, Mülkler (readiness "N/5 hazır" rozeti), **Bilgi Tabanı** (6 hazır şablon çipi),
Şablonlar, Raporlar (donut doluluk + AI risk görünümü + risk türleri), Ayarlar (+ "Takvim Akışı Gizliliği" =
iCal feed'de misafir-adı gizleme toggle'ı, default gizli; + Otomasyon'da "Geç çıkış / uzatma teklifi" serbest
metni = boş değilse AI teklifi ödeme-nötr iletir, m37). Operatör paneli: müşteri
yönetimi + impersonation + **Lead mini-CRM** (status/note/followUpAt + WhatsApp linki). Landing: 3-seviye
dürüst senaryo kartları + env-gated canlı AI demo + injection çipi. QR concierge: rezervasyon-penceresi
(aktif konaklama boyunca), secret-free KB, escalate. Returning-guest rozeti (guest.id ile). KVKK: export,
retention/anonimleştirme, orphan-sweep, kayıt onay kutusu.

## ÇALIŞMA TARZI (kalıcı tercih — kullanıcı "ezberle" dedi)
- **CODEX PROTOKOLÜ (kullanıcı, 2026-07-11 — ÇOK ÖNEMLİ):** Codex'in dediklerini ZORLA yapmak zorunda değilsin.
  Doğruysa/mantıklıysa yap; sen daha iyisini biliyorsan GEREKÇENİ AÇIKLA — kullanıcı bunu Codex'e iletir, Codex
  "senin dediğin gibi olsun" derse öyle kalır. Yani: körü körüne uygulama YOK, gerekçeli ret MEŞRU, son karar
  karşılıklı gerekçe üzerinden.
- **BOL AGENT, DURMADAN:** her iş turunda 8-12+ paralel agent (FE/BE/güvenlik/hız/müşteri-gözü/strateji),
  bulguları KOD İLE DOĞRULA (agent ~yarı bulguda yanılır), sadece gerçek+güvenli olanları uygula, sonunda
  tek "karar listesi" sun, soru sorma. ⚠️ Agent raporları uzun → ajanlara "sadece gerçek bulgu, kısa
  format" de (context şişmesin). Limit yakılırsa bulguları CLAUDE.md'ye "sıradaki oturum" olarak yaz.
- **İletişim:** klişeye takılma, önce düşün sonra yaz, ürünü aynı üçlü kalıpla tekrarlama (robotikleşir),
  varyasyon getir. Kararı kullanıcıya değil, uygula ("sen yap").
- **Persist:** önemli kararlar repoya (CLAUDE.md) yazılır — ephemeral web ortamında en güvenilir hafıza.

## 📌 KALICI KARARLAR & MEKANİZMALAR (yeniden tartışma / geri alma YOK — gerekçeler git log'da)
**Migration:** 24 klasör `00_init→23_reservation_amount_decimal`, zero-pad, sıfır-drift doğrulanmış. Test harness `db push` kullanır → migration-chain drift'i TESTTE YAKALANMAZ; yeni migration'da taze PG'de 00→N `migrate deploy` + `migrate diff --exit-code` ELLE (CI'da migration-chain job'ı da var).
**Billing/Paddle (tamam — dokunma):** previewToken HMAC + `jti` TEK-kullanım (SystemLock nonce; başarısız apply release eder) · apply Paddle'a dokunmadan önce tutarı YENİDEN preview edip eşleşme şart (sapma→409 amountChanged) · Paddle hatası **definitive(4xx)=release+retry** vs **ambiguous(5xx/408/network)=nonce tüketik kalır, ASLA ikinci PATCH** → GET-reconcile, değilse 202 pending (webhook settle) · pending kilidi `plan-change-pending:{org}` (atomik claim, holder=hedef priceId; settle yalnız guard-uygulandı VE priceId==holder) · consent freshness imzalı `occurred_at`'e göre (geç retry entitlement kaybettirmez) · `resolveOrgId` providerRef-first, sonra consentId; ham `custom_data.organizationId` sadece legacy fallback (client-supplied, org-binding CONSENT üzerinden) · **CheckoutConsent'e consumedAt/single-use EKLEME** — transaction.completed + subscription.created AYNI consentId'yi sırasız paylaşır, tüketirsen müşteri öder ama entitlement alamaz · webhook occurred_at vetosu `updateMany` WHERE'inde (atomik; bayat event taze durumu ezemez); 5xx→Paddle retry, invalid-sig→401, unparseable→200 · Invoice `@@unique([provider,providerRef])` + `Subscription.customerId` (erasure eşleşmesi) · canceled abone yeni checkout açabilir (`manageable` status!=canceled) · **Paddle hosted portal PLAN DEĞİŞTİRMEZ** (yalnız iptal/kart/fatura — gerçek ₺30 ödemeyle öğrenildi) → in-app upgrade/downgrade `PADDLE_PLAN_CHANGE_ENABLED` (default OFF) + preview-önce-onay (upgrade'de tutar yoksa buton disabled); downgrade HEMEN geçer, fark sonraki faturaya (metin öyle); **upgrade+downgrade KULLANICI HESABINDA CANLI DOĞRULANDI (2026-07-13, flag=1 kalıcı)** · trialing+trialEndsAt=NULL=EXPIRED · founder guard: PRIMARY_ORG_ID asla paywall'lanmaz · grace çapası pastDueSince (ASLA updatedAt).
**DB unique hakemleri (migration 18-20, prod ÖNCE temizlendi):** `Subscription @@unique([provider,providerRef])` (kompozit — Invoice emsali; webhook `orgFromProviderRef` artık `provider:"paddle"` pinli) · `Message @@unique([conversationId,externalId])` · `Reservation @@unique([propertyId,sourceReference])` (NULL'lar distinct → manuel/trial satırlar serbest). **P2002 deseni:** `isUniqueViolation(err, cols)` (db-errors.ts) YALNIZ hedef constraint'i dedupe-hit sayar; yabancı P2002 fırlar. Persist-sonrası dedupe-hit'te konuşma YİNE answered işaretlenir (yoksa claim expire → misafire İKİNCİ mesaj). iCal'de dedupe-hit=skip (başka source'un satırına dokunma). **Task.origin (m17):** manual|system|ai; iptal temizliği YALNIZ system siler; DB default manual=fail-safe; backfill sinyal-bazlı (belgeli residual: başlığı elle değişmiş eski oto-görev manual kalır → iptalde silinmez). **Prod dupe temizliği kaydı (2026-07-13):** pg_dump + preflight → TX-1 (1044 mesaj çifti silindi) + TX-2 (114 rezervasyon çifti birleştirildi: 43 conv + 4 task keeper'a taşındı, amount/currency/guestExternalId coalesce, en-eski-satır korundu) → her ikisi recheck=0.
**Outbound (mesaj gönderimi):** claim-then-send HER yolda (auto/manuel/QR-host) · claim TTL 120s (Hospitable worst-case ~87s) · definitive→release+retry, ambiguous(timeout/5xx)→claim TUTULUR + "konuşmayı kontrol etmeden tekrar gönderme" 502 · claim-store hatası fail-CLOSED 503 · `sendOnChannel` provider mesaj-id döndürür → `externalId`; sync **adopt-and-heal** (aynı-gövdeli id'siz outbound satırını iyileştirir, inbound asla) · translate FAIL-CLOSED: çeviri başarısızsa mesaj GÖNDERİLMEZ (translate() yapılandırılmış sonuç; LRU 200/6h, input 6000 cap).
**Sync motoru:** SystemLock fencing token + TTL 15dk (bilinçli — deep-sync ortada expire olmasın; sorun olursa heartbeat'e geç) · deep-cadence SystemLock'ta (tüm replikalar tek takvim) · reservation UPDATE'te guestName KORUMALI (Airbnb maskesi gerçek adı ezmesin; placeholder sadece create) · retention resurrection guard'ları: scrubbed satıra (ANON_NAME/ANON_ID; konuşma guard'ı REZERVASYONUN ANON_NAME'ine bakar) PII geri yazılmaz + retention-cutoff'tan eski mesajlar re-import edilmez (`retentionCutoff()` tek-kaynak) — ⚠️ KAPSAM (Codex, araştırma-doğrulanmış): bu guard'lar YALNIZ **süre-bazlı** anonimleştirme politikasıdır (6698 m.7 resen imha); "cutoff-sonrası yeni mesaj normal import" davranışını genel KVKK kuralı diye SUNMA. Misafirin AÇIK silme talebi (m.11; Yönetmelik m.12) AYRI rejim: silme tanımı "tekrar kullanılamaz" (Yön. m.8) → sync-diriltmeye karşı kalıcı ErasureTombstone şart; tasarım draft §8'de, [HUKUK KARARI] bekliyor, kod YAZILMADI · linkProperty P2002 log-and-continue · lastMessageAt cursor idempotency.
**iCal:** satırlar kaynağa bağlı (`Reservation.calendarSourceId`; legacy-NULL adoption ATOMİK updateMany) · disappearance-reconcile KAPALI (mass-cancel dersi); STATUS:CANCELLED yalnız KENDİ source satırını iptal eder · fetch: `redirect:"manual"` (0 redirect izlenir → re-validate gereksiz) + 15s timeout + isPrivateHost + fetch-anı `resolvesToPrivate` ön-filtre + **DNS-rebind PIN** (`fetchFeedText` node:https/http + native `lookup`, undici DEĞİL — engine/major-mismatch/container-smoke belirsizliği yok: socket YALNIZ doğrulanmış-public IP'ye bağlanır, TLS SNI/sertifika değişmez; çoklu-yanıtta biri private ise TÜM bağlantı reddedilir, IPv4-mapped dahil; her istek kendi socket'i (`agent:false`=pool yok, rebind sonrası socket-reuse imkânsız), `end`'e kadar tam tüketilir; **timeout=TOTAL wall-clock deadline** (idle değil — slow-drip DoS'u keser) + socket idle timeout) + **streaming 10MB byte-cap** (readBodyCapped) · HTTPS zorunlu (create + SYNC ikisinde; #22: http feed URL'i plaintext credential sızıntısı + TLS-yok rebind → sync artık http'yi de reddeder, legacy http satırı varsa host https'e geçmeli; test transport http'ye izinli) · parser STATUS:CANCELLED okur → yerel iptal + auto-task silinir.
**AI güvenlik (prompts/fallback/gate üstüne ek):** gate `context {history, guestName}` alır — YALNIZ deterministik injection detektörü koşar (risk netleri last.body'de kalır) · `statedCheckoutTime` misafir mesajında DETERMİNİSTİK kanıt şart (`ai/stated-time.ts`; halüsinasyon rezervasyona yazılamaz; TR harflerinde `\b` YOK → `(?:^|\s)`) · usedSources whitelist: property {checkInTime,checkOutTime,name,city,address-varsa} + reservation {guestName,arrivalDate,departureDate,status}; "history" stil-rehberini DE kapsar (prompt sözleşmesi — gevşeklik değil) · çıktı kapları: max_tokens 900 (reasoning 2000), reply/risk/action 2000/300/300, detectedLanguage normalize · guest-name `sanitizePromptValue` · **52KB prompt kısaltma RED** (prompt-cache aktif; kısaltma=hot-path rekalibrasyon) · landing demo `wouldAutoSend` = GERÇEK gate (server-side) · truncated(finish_reason=length)→fallback.
**QR concierge:** per-stay device binding (first-scan atomik claim `chatBoundHash`, rezervasyon başına rotasyon; farklı cihaz→mismatch, geçmiş+model+DB YOK) + host "cihaz kilidini sıfırla" endpoint'i · `isOpenNow` giriş/çıkış SAATİ kapısı simetrik (çıkış günü checkout saatinden, giriş günü checkin saatinden önce/sonra kapalı) · gate `premiumAllowed` (dormant-safe) · mustEscalate backstop'ları (injection/safety/rule_violation/discrimination/human_request/ad) · tavizler: turnover boşluğunda erken tarayan claim=DoS (sızıntı değil) — host reset çözer.
**Auth/RBAC:** sessionEpoch + impersonation actorSessionEpoch · `requireSession` DB fail-CLOSED (throw→null) + DB-yetkili role/org; `requireAuth` oturum fail-open AMA capability staff'a CLAMP · staff 3-katman: ~17 route withManage · task assigned-only + checklist yalnız `done` toggle · middleware exact-`/tasks` · e-posta doğrulama ATOMİK tek-kullanım (updateMany hash-hâlâ-set) · OAuth state {state,orgId,userId} taşır; callback exchange'den ÖNCE session eşitliği (`context_changed`) · **rol değiştiren yüzey YOK** — ekip-yönetimi eklenirse sessionEpoch bump ZORUNLU ŞART · 2FA TOTP atomik burn + timingSafeEqual, fail-closed.
**Boot/deploy/health:** prestart `scripts/verify-env.mjs` = TEK env-doğrulama kaynağı (instrumentation'a doğrulama KOYMA — "Ready ama 500" bug'ıydı); prod'da eksik/placeholder AUTH_SECRET veya eksik/AUTH_SECRET'a-eşit ENCRYPTION_KEY → non-zero exit · railway.json `healthcheckPath=/api/health` (200 gelmeden trafik yok; **startCommand ASLA ekleme** — Dockerfile migrate+prestart zincirini atlatır; pin testli) · `/api/health` normal=readiness (yalnız DB), `?strict=1`=ops (heartbeat eksik/unknown/15dk+ → 503; no-store; reason) — heartbeat=SystemLock("scheduled-sync").updatedAt, iş-yapmayan geçişler de sağlıklı · `guard-local-db.mjs`: db:reset/db:push loopback şart (ALLOW_PROD_SEED=1 kaçış) · seed prod-guard DATABASE_URL-local · CI push-only (main+deploy branch): test + migration-chain + build + e2e smoke (Chromium `/opt/pw-browsers/chromium`).
**KVKK:** `redactSensitive` alan-duyarlı + quoted-branch (virgüllü değer) — code/id/status bare kalır · webhook erasure: custom_data.organizationId + `Subscription.customerId` eşleşmesi; pass-2 BAŞKA org'un satırına asla; redakte satır status:"processed" · outbound gövde ad-redaksiyonu (`\p{L}` lookaround, <3 harf atlanır) · iCal feed'de misafir adı DEFAULT GİZLİ (`icalShowGuestName`) · foto-temizlik hatası reportError'a gider (yutulmaz) · trial-mail başarısızsa claim geri alınır (retry).
**Görev/Supply (Faz A):** akıllı görev İKİ escalation yoluna da bağlı (keyword+model), opt-in `autoTaskFromMessageEnabled` (default OFF), dedupe `{property}:{type}:{topic}:{İst-gün}`, task-only netler GATE'İ BESLEMEZ · supply: profil per-daire × giriş sayısı (AI/misafir-sayısı YOK), stok org-level net (`toBuy=max(0,ihtiyaç−elde)`), +1 `SupplyRequest` opt-in `autoSupplyRequestEnabled` + `@@unique([sourceMessageId,itemKey])`; tavizler: +1 satırı 7g sonra sadece rapor-dışı (silinmez), stok elle güncellenmezse kayar · checklist `buildSupplyChecklist` yalnız YENİ cleaning görevine · Task `@@unique([reservationId,type])` BİLİNÇLİ YOK (manuel 2. görevi bloklar).
**SEO/landing dürüstlük:** (app) layout noindex + robots TÜM (app) dizinleri (fs-drift pin testi) · sitemap /login yok + SABİT lastModified (landing=son içerik değişimi, legal=LEGAL_VERSION) · twitter card "summary" (görsel asset'i yokken large_image bozuk) · TRUST çipleri: "KVKK odaklı tasarım" + "Şikayeti otomatik sonuçlandırmaz" (mutlak iddia YOK) · FAQ opt-in holding-ack'i açıkça söylüyor · fiyat kartları YALNIZ daire sayısı+destekle ayrışır (özellik kilidi YOK — kullanıcı kararı).

## ✅ TARİHÇE — kapanmış turlar (tek satır; ayrıntı git log + commit mesajlarında)
- 07-04: 10-ajan sweep (5 fix) · 8-ajan 1-3 (sync fencing m6 · pastDueSince · occurred_at) · KVKK iCal ad-gizleme (m7).
- 07-05: derin denetim tur-1..5 (kelime-ağı boşlukları · TOTP timing · sync BUG1-3 · impersonation epoch · trial-mail rollback · QR turnover/wifi-secret · CSV sertleştirme · operatör-müşteri login · rule_violation+discrimination netleri · riskLevel fail-closed · seed guard · SEO canonical) · KVKK batch (Sentry redaksiyon · resurrection · webhook PII · "Bu Gece Kalan" · ad-redaksiyonu) · consent-evidence m8 + checkout-consent m9 + fail-closed + legal metinler + #44 KVKK UX + audit-label.
- 07-10: pre-launch 42-ajan workflow (94 bulgu; HIGH: redactSensitive quoted) + migration zero-pad m00-09 prod-fix · akıllı görev Faz A (m10) · hazırlık/supply (m11-13) · 40-ajan lansman + codex örtüşmesi → FAZ-1+2A+2B (requireAuth/requireSession DB-yetkili · 2FA/TOTP · KB-cap · iCal SSRF · consent planCode · founder guard · photoUrl scheme · QR gate) · QR device binding (m14) + reset · staff RBAC 3-katman · Paddle portal v1 + İN-APP plan-change (gated) · codex 3 boşluk (checkout org-binding · canceled kilidi · preview-siz onay) · B3 previewToken HMAC + A1 capability clamp.
- 07-11: 3 billing nüansı (jti tek-kullanım · tutar re-preview · occurred_at freshness) + 2 gap (definitive/ambiguous · providerRef-first) = billing TAMAM · backlog büyük turu 8 commit (manuel-reply claim · adopt-heal · KVKK customer_id m15 · gate geçmiş+ad · Invoice unique · DNS-SSRF · boot fail-fast · İstanbul-ay · rezerve senderName · CI kapıları) · codex kapanış (D2 atomik + E2E smoke — seed emailVerifiedAt regresyonunu İLK koşuda yakaladı) · codex tur-2+4 (staff /tasks sızıntı · iCal mass-cancel m16 · outbound ambiguous · pending kilidi · upload limit · consent withManage).
- 07-12/13: boot-gate #3 (prestart; canlı doğrulandı — AUTH_SECRET placeholder outage'ı bunun eksikliğindendi) · ENCRYPTION_KEY zorunlu · host-header injection (appBaseUrl) · QR saat-kapısı · fiyat metni hizası · #5 strict health (`cbf26f5`) · **codex 40-madde salt-okuma turu: 12 commit** (#4 healthcheck `c4b4cd5` · #9 db-guard `3c2e77a` · #11 verify-atomik `c79bc1b` · #13 OAuth-bağlam `2bd1c18` · #27 demo-verdict `7331915` · #28/29 kanıt `882df5e` · #30 translate `7e64b83` · #7 foto `c560cdb` · #22 iCal `89dade9` · #36-40 dürüstlük/SEO/docs `4d96ffc`); gerekçeli RED: #12 (rol-değişim yüzeyi yok) · #28-history (sözleşme) · #31 (prompt kısaltma). · docs sadeleştirme `4d19b5c` (CLAUDE.md 884→181) · **kolay-kalanlar batch'i:** #34 export metni gerçek allowlist'e (`f923a91`) · #17b mülk-limit yarışı post-create mutabakatı (`735d714` — yarış tek-process'te reprodüse OLMADI, guard multi-replica savunması; test invariant pinler) · #21-lite feed-URL ekran maskesi + account-card field-first (`26af98f`).
- 07-13 (2): **migration turu (m17-21, `6bd46c2`+`aabdf74`):** Task.origin + 3 unique + verify-index + hedefli P2002 companion; taze 00→21 zero-drift + negatif kanıt (dupe veride m19/20 REDDETTİ) + dolu-fixture backfill; 945 test. Task@@unique gerekçeli DIŞARIDA (done→aynı-gün yeniden-açma).
- 07-13 (3): **RiskEvent (m22, `4dfc935`):** append-only karar geçmişi, YALNIZ nihai kod-kararından sonra (dryRun yazmaz, asla fırlatmaz); kapalı-set clamp'ler, keyword yolunda riskLevel=NULL, org FK CASCADE=KVKK, rapor kartı geçmişten sayar.
- 07-13 (4): **para Faz-A (m23, `887e2e9`):** Reservation.totalAmount→DECIMAL shadow `totalAmountDec` (expand-only); okuma Dec-öncelikli (money.ts), rapor geliri Prisma.Decimal. **PG tuzağı: NaN=NaN TRUE** → backfill/healing isimle eler. Cutover healing her sync'te kilitten HEMEN sonra. Faz-B önkoşulu prod reconciliation=0 ✅CANLI.
- 07-14 (1): **Durable Outbox #8 (m29, `23113b6`+`b1ff1f0`+`acdc962`):** enqueueOutbound tek-tx + worker SKIP LOCKED state machine + bounded backoff/seeded-jitter. Codex 7-boşluk (flag-OFF drain · per-conv FIFO+tek-uçuş · UI rozeti · tutucu reconcile · manuel+AI bağlı · teslimde-answered · tenant-izolasyon). Bug: settle() Message.externalId link'inden ayrıldı. Ayrıntı DEPLOYMENT.md §6.
- 07-14 (2): **UZUN OTONOM TUR — FAZ 0-3 + review-fix (`9c70168`·`f50d5fb` m30·`bfa6a1c` m31·`7c97d0a`·`731bddc`):** outbox TAM kapsam (holding-ack + welcome/checkin/checkout via `enqueueProactive`, *SentAt teslimde) + #23 feed-disappearance (m31) + retention TASLAK. CI 4/4.
- 07-14 (3): **Codex tur-6+7 outbox-lifecycle (`e5e4c35`+`14c5d0a`, MIGRATION YOK):** (6) *SentAt yalnız doğrulanmış teslimde (review sahte-damga kaldırıldı) + rollback FENCE `lifecycleOutboxOwns`. (7) **Hospitable 402 = kalıcı `blocked`** (ayrıntı ↑Durum). Kırmızı-önce 7-adım test.
- 07-14 (4): **Outbox ops `/sent/queue` (`e6364a1`, MIGRATION YOK):** owner/manager PII'siz liste + yalnız failed[≠402] tenant-bound retry (`requeueFailedOutbox`); `failed→pending` İNSAN-yolu geri (ayrıntı ↑Durum). 14 test.
- 07-14 (5): **Private storage S3/R2 TEMELİ (`323ca3c`+`9d78be5`+`d8a33ae`, m32, flag `STORAGE_ENABLED` DEFAULT KAPALI):** elle SigV4 + imzalı serve + idempotent silme-kuyruğu + flag-OFF=legacy; StorageDeletion org FK'sız (cascade'den sağ). Review ORTA bulgu: silme choke-point + PATCH org-guard. Ayrıntı DEPLOYMENT.md §7. 31 test.
- 07-16 (7): **Codex derin denetim turu — 6 fix (MIGRATION YOK):** (P0 TENANT) durable outbox worker: org enqueue SONRASI bağlantı kaybederse `getOrgHospitableToken`→null→`undefined`→hospitableFetch env(founder) token'ına düşüyordu = CROSS-TENANT gönderim. Fix: processOne token yoksa provider'a HİÇ gitmez, satır `pending`+backoff'a park (`lastErrorKind:disconnected`, attempt harcanmaz), reconnect'te teslim. Test: tokenFor→undefined'da send çağrılmaz. (P1 FEED) suspicious-drop bazı DÜŞÜK sayıya çekiyordu → karantina kendini bozuyordu (100→10 bir kez atlanır ama baz=10, sonraki 10 "güvenilir" → 90 kayıp iptal edilebilir). Fix: suspicious'ta baz KORUNUR (yalnız lastReconcileAt); sustained-partial FULL feed dönene dek asla iptal etmez (mass-cancel=asla). Test: 100→10×3 tur 24s+2-miss sonrası bile iptal=0. (P2 AUDIT) Claude denetçisi gönderilmemiş outbox taslaklarını (pending/failed/canceled/review) "gönderilmiş" sanıyordu → reports FAZ-0 emsali: teslim-edilmemiş messageId'ler notIn. (P2 PROPERTY-PII) iç mülk adı ("Serdar'ı Ekrem 2") Claude'a ham gidiyordu → oturuma-özel takma ad (Daire-1/2...). (P2 STORAGE) `isAcceptablePhotoUrl` yalnız org segmentini doğruluyordu → taskId segmenti de eklendi (org-içi çapraz-görev foto-bağı = A'nın foto'su B'ye yazılıp B silinince A'nın objesi silinirdi). (P3 ATOMIK) ChatUsage delete org-delete TX'inin DIŞINDAYDI → TX içine alındı. **DEFER (gerekçeli):** #3 StorageObject manifesti (pending→attached→deletion_pending yaşam döngüsü + abandoned-upload orphan süpürme) — GERÇEK boşluk ama ağır (yeni tablo + lifecycle); STORAGE_ENABLED default-OFF + bucket YOK → "bucket açma" productionization turuna ait, hotfix değil.
- 07-16 (6): **Codex gölge-sertleştirme turu (MIGRATION YOK):** (#2) dedupe CLAIM-FIRST — satır ("pending") model çağrısından ÖNCE yazılır, P2002=Akash'a çifte istek/veri aktarımı YOK (test tek-fetch asserte eder); yarım kalan süreç satırda "pending" olarak görünür. (#4) HTTPS zorunlu (http base URL → modül pasif + throttled log) + yanıt gövdesi 64KB byte-cap (readBodyCapped, iCal emsali). (#3-kısmi) tavan sayım-claim arası dar yarışta tek-haneli taşabilir (BİLİNÇLİ — maliyeti önemsiz, dedupe atomik) + YENİ `SHADOW_AI_ORG_IDS` allowlist'i (pilot tek org'a pinlenebilir; per-org kota REDDEDİLDİ — tek gerçek müşteri var). (#5) nezaket kapanışı da gölgeye bağlandı (whitelist yanlış-pozitif dedektörü; testli). (#1-kısmi) admin kartı "gönderdi"→"onayladı" + "kapı kararı ≠ teslimat" notu; teslimat-takibi (outbox settle→ShadowVerdict) REDDEDİLDİ — pilot SINIFLANDIRICILARI kıyaslar, teslimatı değil; worker vetosu (iptal/pencere) mesaj riski hakkında bilgi taşımaz. **(devam) VERİ MİNİMİZASYONU:** misafir mesajı Akash'a gitmeden `redactNameFromBody`+`redactSensitive`'den geçer (ad→[Misafir], telefon→[PHONE], e-posta→[EMAIL], uzun kod→[NUM]); risk anlamı korunur (şikayet/para kelimeleri kalır), 5 çağrı `guestName`=conversation.guestIdentifier taşır, test fetch-body'de ham ad/telefon/mail YOK'u asserte eder. Stale-pending: yarım kalan claim satırı KÖR RETRY tetiklemez (dedupe) → admin kartında "yarım" etiketi (testli belge).
- 07-16 (5): **Codex 3'lü güvenlik turu (MIGRATION YOK):** (P1a) şifre-değiştir + forgot-password: kod tüketimi + şifre yazımı TEK koşullu updateMany'ye alındı (WHERE doğrulanan hash'i pinler — paralel iki confirm'den yalnız İLKİ geçer, last-writer-wins ve çifte epoch bump bitti; email-verify atomik-consume emsali; iki yarış testi Promise.all ile [200,400] asserte eder). (P1b) QR konuşma yarışı: tablo-geneli @@unique REDDEDİLDİ (aynı rezervasyonun birden çok GERÇEK Hospitable thread'i meşru + dolu-tablo unique riski) → bunun yerine QR konuşması DETERMİNİSTİK PK (`qrconv_{reservationId}`) ile açılır, çakışan P2002(id) yakalanıp kazananın satırı kullanılır (kaybeden mesaj kaybetmez; eski rastgele-id'li QR satırları findFirst ile bulunmaya devam eder). (P2) CSP iki katman: ENFORCE yalnız kırılamaz direktifler (`object-src 'none'; base-uri 'self'; frame-ancestors 'self'`), tam politika (script-src dahil) Report-Only kaldı — script-src enforce'u Next inline bootstrap için nonce altyapısı ister (ayrı tur), nonce'suz enforce ya işe yaramaz ('unsafe-inline') ya paneli kırar. (m36 `ShadowVerdict`, flag `SHADOW_AI_ENABLED` DEFAULT KAPALI):** ikinci model (Akash OpenAI-uyumlu endpoint, default `zai-org/GLM-5.2` — supply-ai ile AYNI altyapı/anahtar: SHADOW_AI_* yoksa SUPPLY_AI_*'a düşer) her auto_reply gate kararında misafir mesajını BAĞIMSIZ sınıflandırır (allow|hold|escalate + riskType RiskEvent kapalı setine clamp) ve hükmü nihai kod-kararının YANINA yazar. **KARAR YETKİSİ SIFIR:** `void recordShadowVerdict(...)` (await yok — gönderim 1ms gecikmez), ASLA fırlatmaz (arıza satıra redakte `error` olarak yazılır), yalnız ShadowVerdict'e yazar, dryRun'da hiç çağrılmaz (4 kanca recordRiskEvent'in dibinde — alerts/keyword yüzeyi bilinçli DIŞARIDA, orada model kararı yok). At-rest PII YOK (mesaj gövdesi modele gider, tabloya yazılmaz — test asserte eder). Pilot tavanı `SHADOW_AI_SAMPLE_CAP` (default 200) dolunca sessizce durur. `agrees` kolonu: (verdict==allow)==(gate==auto_sent). /admin "GLM Gölge Pilotu" kartı: uyum % + "GLM daha sıkı/gevşek" sayıları + son 10 satır. GLM reasoning kapalı (`chat_template_kwargs.enable_thinking:false`), temperature 0, timeout 15s. 8 entegrasyon testi (kapalı-default, clamp, hata-yutma, tavan, dedupe, KVKK). Akash da ikinci veri işleyen → DPA notu LEGAL listesinde geçerli.
- 07-16 (3): **Dağıtık rate-limit (m35, `7ce8e5f`) + org-timezone turu:** (a) RateLimitCounter tablosu — otorite Postgres, hit başına tek atomik INSERT..ON CONFLICT (`now() AT TIME ZONE 'utc'` — session-tz tuzağı yok; süresi dolan pencere yerinde sıfırlanır), karar count<=limit; DB hatasında ESKİ bellek sayacı fallback (koruma kapanmaz, dakikada 1 throttled log); 38 call-site async; sweep deep-sync'te; paralel-atomiklik testli (10 eşzamanlıda tam 5 geçer). (b) org.timezone DOĞRULUK katmanı: `src/lib/timezone.ts` TEK kaynak (orgTimezone clamp'i — bozuk değer Intl'i patlatamaz, Istanbul'a düşer; zonedDayRange/dateKeyInTimeZone/currentHourInTimeZone automation'dan TAŞINDI, automation re-export eder; YENİ `zonedDateStart(y,m,d,tz)` — probe-date/sabit-offset ay matematiği UTC'nin BATISINDA gün kaydırıyordu, raporlar+cancellations ona geçti). Bağlanan yerler: welcome/checkin/preview'lar + görev-backfill + smart-task dedupe günü + supply planı + QR açık-saat kapısı + reports (5 fonksiyon) + tasks/cancellations sayfaları. Ayarlar'a "Saat Dilimi" kartı (Intl.supportedValuesOf listesi; sunucu kapalı-set doğrular, uydurma→400). Ayırt edici test: NY-gününe-2-saat-kala varış NY org'da listelenmez, Istanbul org'da listelenir (previewWelcomes + getPrepPlan). Görüntüleme damgaları (utils.ts) bilinçli Istanbul kaldı. MIGRATION yalnız (a): m35, taze PG 00→35 sıfır-drift.
- 07-16 (2): **Claude kalite ÜST-DENETÇİSİ v1 (gölge, MIGRATION YOK):** operatör paneli kartı + `POST /api/admin/quality-audit` (super-admin only) + `src/lib/quality-audit.ts`. Rol mimarisi (kullanıcı direktifi): **gpt-5.1 üretir → Claude YALNIZ denetler → rapor + prompt/test ÖNERİR → insan onaylar → prompt elle güncellenir.** Claude mesaj GÖNDEREMEZ, prompt DEĞİŞTİREMEZ (salt-okuma; route yalnız audit-log yazar). Örneklem: son N gün gönderilmiş AI yanıtları (authorType-öncelikli + legacy senderName + aiAssisted) + önceki misafir mesajı, ≤60, gövde 700 tavan; **PII uygulamadan çıkmadan redakte** (`redactNameFromBody` [artık export] + `redactSensitive`; test SDK'ya giden istemde ham ad/telefon YOK'u asserte eder). Boş örneklem=API çağrısı YOK. Model çıktısı kapalı-set clamp'li zod şemasıyla doğrulanır (severity/criterion; bozuk=dürüst 502). SDK `@anthropic-ai/sdk` 0.111 (resmî TS SDK; model default `claude-opus-4-8`, env `QUALITY_AUDIT_MODEL` ile değiştirilebilir; adaptive thinking; timeout 120s/1-retry). **ZOD 3.24.1→3.25.76 bump'ı** (SDK peer şartı; v3 API birebir korunur, v4 ayrı namespace — 1327 test + build ile doğrulandı). Env: `ANTHROPIC_API_KEY` (kullanıcı Railway'e ekler; yokken kart pasif metin gösterir, boot etkilenmez). Audit etiketleri: admin.quality_audit + admin.2fa_reset (eksikti). DEPLOYMENT.md §8.
- 07-16: **Codex görsel/UX turu-2 (karar listesi chat'te):** iyzico TAMAMEN KALDIRILDI (rota+client+test — Paddle MoR kilitli, yarım shared-secret auth tuzaktı; geri gelirse GERÇEK imza doğrulamasıyla sıfırdan) · onboarding 6. adım GERÇEK aktivasyon (toggle+premiumAllowed+AUTO_REPLY_ENABLED üçü birden) · test harness TEST_DATABASE_URL bypass (Windows/macOS: Linux provisioning atlanır, yalnız db push) · ci.yml Wait-for-CI yorumu iddiasız yapıldı. Gerekçeli RED: zoom 0.95 geri alma (kullanıcı A/B ile seçti; token-bazlı yoğunluk = her sayfada rekalibrasyon) · lint şimdi değil (backlog'a) · onboarding 5. adım "keşfetti" proxy'si kaldı (gerçek ölçüm=analitik gerektirir). **KULLANICI GERİ ALDIRDI (aynı gün): sidebar 3-grup + Panel "Dikkat gerektirenler" bandı — beğenilmedi, düz menü + bandsız panel geri geldi; TEKRAR ÖNERME.** Sekmeli Ayarlar SONRAKI TUR (backlog'da, deep-link taşıma şart).
- 07-15: **landing içerik turu (`0efcc23`+`c735447`):** karşılaştırma bölümü + kurucu kartı (Musa Çınar) + kırmızı-çizgi listesi + e-posta bildirimi açık + 2 SSS; calendar-sync fixture tarih-bombası dinamikleştirildi (`0e0b7e5`). **Nezaket kapanışı (m33, `c64714b`):** org toggle `autoClosingReplyEnabled` DEFAULT KAPALI — açıksa salt-teşekkür kapanışına misafir dilinde TEK deterministik "Rica ederiz!" (holding-ack mekaniği: kapılar+claim+outbox; ping-pong guard aiIntent=closing_courtesy; emoji→org dili). `/api/ai/test` İMZA PARİTESİ (önizleme=gerçek gönderim; not bilinçli hariç). **m34 `closingReplyText`:** host kendi nezaket satırını yazabilir (doluysa HER dilde verbatim; boşsa kısa dil-varsayılanı "Rica ederiz, iyi günler dileriz! 😊"); test kartı kapanışta GERÇEK gidecek mesajı gösterir (`composeClosingCourtesy` tek-kaynak, `closingReplyPreview`). **Salt-övgü (positive_feedback) nezaketi (`a5090c4`+`d43b249`, MIGRATION YOK):** `isPositiveFeedback` **WHITELIST** tabanlı (Codex sertleştirmesi — deny-list'i "kapı kilidi açılmadı"/"çocuğumuz düştü" GERÇEKTEN bypass ediyordu; artık HER token kapalı sözlükte + ≥1 övgü-anchor şart, TEK bilinmeyen kelime → model+kapı): aynı toggle/özel-metin, praise varsayılanı zaman-bağımsız "Güzel geri bildiriminiz için teşekkür ederiz." (6 dil), nezakette otomatik-not YOK (imza korunur), nezaket-sonrası övgü/teşekkür HER İKİ türde SESSİZ (modele düşmez), Codex 6-sınıf paralel taksonomisi gerekçeli RED (mevcut intent+kapı zaten o iş; A-seçeneği "önce model kapısı" de RED — kapı model çıktısını değerlendirir, her teşekküre model çağrısı olurdu). Kırmızı-önce: Codex 4 vakası + injection-övgü + praise-trap-iade + retry/paralel-worker/courtesy-after-courtesy/host-müdahale. 1302 test.
- 07-17: **3 golden risk deseni (`64be155`):** öz-zarar/kriz (→safety_emergency, NÖTR taslak — kriz-danışmanlığı YOK, host'a devir; müşteri kötüye kullanabilir), squatting/çıkışı-reddetme (→rule_violation, `OVERSTAY_REFUSAL_PHRASES` red-çapalı; NORMAL uzatma değil), kodlanmış injection (base64/ters-metin INJECTION_PATTERNS). Her birine tehdit+tuzak golden. **Geç çıkış / uzatma upsell (m37 `37_late_checkout_offer`, `a5a6bae`):** `Organization.lateCheckoutOfferText` (nullable, boş=bugünkü davranış); host kendi fiyat/koşul teklifini yazar → AI YALNIZ misafir geç çıkış/uzatma sorunca teklifi ev sahibinin teklifi olarak iletir (Bölüm 7.5 "fiyat yazma" kuralına teklife-özel istisna). **ÖDEME-NÖTR:** ne "elden/nakit" ne "platform üzerinden" (Airbnb TOS + host esnekliği); son onay HER ZAMAN host'a (actionSuggestion); teklif `sanitizePromptValue` ile fenced (`<<OFFER_START/END>>`, 400 cap, injection sızmaz); yalnız OpenAI yolu yüzeyler, fallback yine host'a devreder. Bağlı: auto-reply + manuel öner + test kartı (imza paritesi); QR + landing demo bilinçli DIŞARIDA. Yan: GLM gölge testi yarışı düzeltildi (claim-first "pending" satırı — vi.waitFor artık satır VAR + pending'den çıktı bekliyor; ürün kodu değişmedi).

- 07-17 (2): **14-ajan denetim turu (kod-doğrulanmış, 4 commit `9ff5875`·`015a50b`·`d199122`·`c4216f2`, MIGRATION YOK).** Çekirdek (auth/billing/KVKK-redaction/sync-token izolasyonu/storage/QR) TEMİZ çıktı — sömürülebilir P0 yok. Uygulanan güvenli+izole fix'ler: **G1/güvenlik:** SSRF `isPrivateAddress` IPv6 kapsamı (hex IPv4-mapped `::ffff:a9fe:a9fe`=metadata, NAT64 `64:ff9b::/96`, compat `::a.b.c.d`, link-local fe80::/10'un fea0/feb0'ı, site-local fec0::/10 — Node dns.lookup literal'i verbatim döndürüyor; savunma-derinliği, pinned-fetch zaten çoğunu fail-closed yapıyordu) + 4 hospitable preview route (auto-reply/checkin/checkout/welcome-test) `requireSession`→`withManage` (staff misafir-PII görüyordu; staff→403 testi). **G2/AI güvenlik:** isClosingAck negatif-emoji (👎😡 artık "ack" değil), SAFETY_CRITICAL_WORDS TR elektrik/CO (kaçak/kıvılcım/priz/karbonmonoksit), YENİ `LEGAL_THREAT_PHRASES`→complaint (mahkeme/avukat/dava/sue/lawyer), discount-negotiation→refund ("indirim var mı" offer bloğuyla oto-fiyat sızmasın; anchor'lı, "indirimli sezon" tuzağı reddedilir), detectGuestLanguage ö/ü→TR yanlış-etiketi düzeltildi (DE/FR dead-branch), stated-time `(?<!a)ç[ıi]k` ("açık" yanlış checkout-cue), RISK_TYPES tek-kaynak (ai/index→risk-events import), translate finish_reason=length→fail-closed. **G3/KVKK+doğruluk:** offer ödeme-nötr DETERMİNİSTİK backstop (`OFFER_PAYMENT_METHOD_RX` validators.ts, elden/nakit/IBAN→400), admin/export `no-store`, CSV parseAmount TR-binlik ("1.500"=1500, gelir 1000× hatası), reports taskCompletionRate `min(100)`+monthLabel org-tz. **G4/ops:** CRON_SECRET boot-WARNING (yoksa otomasyon sessizce durur, health 200), audit-drop reportError'a (Sentry yorumu yanlıştı), JSON-LD `<`→escape, guest-chat `organization?.`, docstring düzeltmesi. Golden-disiplini: ai-fallback +yeni tehdit/tuzak testleri, private-host/offer-guard/csv testleri. **1390 test yeşil.** Ertelenen kod-doğrulanmış bulgular ↓AÇIK İŞLER "[denetim 07-17 ertelenen]".
- 07-17 (3): **Ertelenen #3/#4/#6 + Codex #4 sertleştirmesi (MIGRATION YOK, PUSH'landı):** koddan doğrulandı, üçü de gerçek bug'dı. **Commit A `84e74e7`** = (#3 KVKK) hospitable-sync CREATE dalına UPDATE'in scrub-guard'ı aynalandı (24ay+ scrubbed stay'e yeni thread → `guestIdentifier=ANON_ID`+`scrubbedThread`, gerçek ad/gövde diriltilmez) + (#4 hata görünürlüğü) import/sync `catch{}`→`catch(err)`+`reportError`, `lastStatus` partial-error'da "ok" DEMİYOR. **Commit B `ac0553c`** = (#6 OAuth) refresh-fail clear'ı koşullu `updateMany` (WHERE `hospitableRefreshTokenEnc=failedToken`) → eşzamanlı rotasyonda kazananın taze token'ı silinmez (lost-update yok; #6 testi regresyon-pini — gerçek async pencere deterministik değil). Her fix'e kırmızı-önce test (KVKK-diriltme / import-hata-görünürlüğü / refresh-yarış). **Codex #4 sertleştirmesi (`9fa657d`):** ilk fix satır-başına reportError çağırıyordu → e-posta throttle'lı ama her satır ayrı Sentry event+log → arıza seli; artık ilk örnek tutulur, döngü sonunda import başına TEK `reportError("import.sync", aggregate)` (yalnız sayı+redakte örnek; ad/gövde/satır verisi YOK); kırmızı-önce 100-satır-HEPSİ-patlar → reportError tam 1 kez + lastStatus=error + mesaj "100 satır" içerir/"Guest" içermez. Tam suite **1410 yeşil** + typecheck + build + taze PG 00→38 `migrate diff --exit-code`=0. **Tümü origin'e PUSH'landı** (A+B+docs+`9fa657d`). ICS/P3 (7-9) DOKUNULMADI.
- 07-18: **P3 "güvenlik + ucuz" temizlik turu — 6 ayrı commit (kullanıcı kapsam seçti; PUSH'landı):** (a `d480e4d`) login user-enumeration → sabit-zaman `dummyVerifyPassword` (user yoksa da bcrypt harcanır, yanıt gecikmesi e-postayı ele vermez) · (b `28baf31`) trusted-device jwtVerify `algorithms:["HS256"]` pin (HS512-forge reddi; session.ts paritesi; açık olsa bile yalnız 2FA-atlama, şifre yine şart) · (c `a5bd6aa`) a11y aria-label'ları (delete-account şifre + calendar-sources 2 input + ai-test select; conversation-thread "Ton" zaten etiketli) · (d `ccf42e4`) importThread mesaj-dedup N+1 → tek batched findMany (all-inbound thread'de message.findFirst 3→0) · (e m39 `726fd00`) `@@index([chatToken])` additive, taze PG 00→39 zero-drift — gerekçe düzeltmesi (Codex): Prisma migrate PG'de migration'ı otomatik TX'e SARMAZ (elle BEGIN/COMMIT gerekir) ve normal CREATE INDEX bitene dek tablo yazmalarını bloklar; m39 Property tablosu KÜÇÜK olduğu için kabul edilebilirdi ve deploy edildi (geçmiş migration'a dokunma). KURAL: gelecekte BÜYÜK canlı tabloya index = AYRI migration'da `CREATE INDEX CONCURRENTLY` değerlendir (TX-dışı şart) · (f `f63d355`) QR concierge gate model riskType HIGH_STAKES etiketine göre de eskale (`HIGH_STAKES_RISK_TYPES` automation.ts'ten export = TEK kaynak; kapıda handoff-ack muafiyeti yok; golden 105 nötr). Her fix kırmızı-önce testli. **ics.ts (#7) GEREKÇELİ ATLANDI** (üç alt-madde de kod-doğrulandı → pratik etki ~sıfır: invalid-date zaten çift-savunmalı, TZID yalnız saatli-event, foldLine yalnız export-kozmetik). Tam suite **1416 yeşil** + typecheck + build + taze PG 00→39 `migrate diff --exit-code`=0. Tümü origin'e PUSH'landı.
- 07-18 (2): **KVKK iki-rejim araştırma turu (Codex tetikledi; resmî kaynaklar: 6698 m.5/7/11/13 · Silme Yönetmeliği m.8-12 · Kurul 2021/1104, 2021/847, 2020/481; MIGRATION/DAVRANIŞ DEĞİŞİKLİĞİ YOK, yalnız belgeleme):** "scrubbed thread'e yeni mesaj normal import" davranışı süre-bazlı politikaya DARALTILDI (hospitable-sync yorum + KALICI KARARLAR kapsam notu — genel KVKK kuralı diye sunulmaz); draft'a §8 eklendi (iki rejim + silme="tekrar kullanılamaz" dayanağı + gerekçeli-red emsali + ErasureTombstone tasarımı: hash-anahtarlı, erasedAt cutoff, ingress guard'ları, yasal-saklama istisnası); misafir açık-silme LEGAL listesine girdi (avukat onayı → ayrı kod turu). **2. faz (aynı gün, kullanıcı "biz doldururuz imza avukata" dedi):** iki açık karar resmî mevzuattan dolduruldu — istisna süreleri (TTK m.82 10y · VUK m.253 5y · MSY m.20/1 3y+ispat-yükü · TBK m.146 10y · Ticari İletişim Yön. m.13 3y) + yeni-veri sınırı (m.7/1 + m.5/2-c,f) + tombstone dayanağı (m.5/2-ç + İYS ret-kaydı emsali); draft §1/5/7/8 güncellendi, [HUKUK KARARI] → [AVUKAT İMZASI].
- 07-18 (3): **m40 GUEST-ERASURE TURU (kullanıcı "başla"; 41. migration `40_erasure_tombstone`, taze PG 00→40 sıfır-drift):** `ErasureTombstone` (org + keyType + HMAC-SHA256 keyHash [ham PII YOK, scrypt-türevli anahtar, tip-alanı ayrımlı] + erasedAt; `@@unique([organizationId,keyHash])` — YENİ tablo, dolu-tablo kuralı uygulanmaz; org-delete Cascade). `src/lib/erasure.ts`: normalize (email lowercase / telefon salt-rakam ≥7) + `buildTombstoneKeys` + `loadErasureGuard` (koşu başına TEK okuma; boş=inert, HİÇBİR ŞEY hash'lenmez) + `previewReservationErasure` + `eraseReservationData` (tombstone + scrub TEK TX'te; scrub = retention sweep'in AYNI sentinelleri [ANON_BODY artık export] → mevcut tüm diriltme-guard'ları erased satırları tanımaya devam eder). **Guard semantiği:** source_reference eşleşmesi = MUTLAK blok (o kayıt silinen verinin ta kendisi, tarih bakılmaz) · guest-key (id/email/telefon) eşleşmesi = YALNIZ erasedAt-öncesi dönem (departure≤erasedAt; tarih yoksa fail-closed) — sonraki konaklama YENİ işleme faaliyeti olarak girer, ama re-used thread'deki pre-erasure mesajlar `erasureCutoff`→`importThread` birleşimiyle yine girmez. iCal: yalnız UID guard'ı (SUMMARY ad-hash'i BİLİNÇLİ yok — adaş yanlış-pozitifi). Host yüzeyi: `GET/POST /api/reservations/[id]/erase` (withManage, `GUEST_ERASURE_ENABLED` default KAPALI, audit "kvkk.guest_erasure" yalnız SAYILAR) + mülk sayfasında iki-adımlı onay kontrolü (dürüst metin: kanal kopyasını Lixus silemez + 30 gün SLA). Kırmızı-önce 19 test: satırlar TAMAMEN silinmişken provider re-send→sıfır re-import (ANON-sentinel guard'ının KÖR OLDUĞU delik — tombstone'un varlık sebebi) · yeni-veri sınırı · era-bloğu · inert-guard · iCal-UID · at-rest ham-PII-yok · flag-off 404 · staff 403 · IDOR · idempotent re-erase. Tavizler (belgeli): CSV/manuel import guard'sız (host'un kendi dosyası) · export'a tombstone tablosu bilinçli eklenmedi (hash-only teknik tablo). Toplam **1435 test**. **Codex 4-şart sertleştirmesi (aynı gün, m41 `41_erasure_tombstone_expiry`):** (1) TOCTOU — executor artık interaktif TX + org-scoped `pg_advisory_xact_lock` (`$executeRaw` — `$queryRaw` void'i deserialize edemez) + **commit-sonrası VERIFY PASS** (`maskReservationRows` idempotent, taze re-query: stale-guard sync'in TX penceresine yazdığı satırları yeniden maskeler; guard-load'un tek findMany'si statement-snapshot → torn-read zaten imkânsız); kırmızı-önce `__afterTxHook` test-dikişiyle yarışan yazım deterministik enjekte edilir. (2) **`ERASURE_HMAC_SECRET`** — dedicated (AUTH_SECRET/ENCRYPTION_KEY'den bağımsız, ≥32, ASLA rotasyon), prod'da fallback YOK (dev/test AUTH_SECRET'a düşer), boot-gate yalnız `GUEST_ERASURE_ENABLED=1`'de (env-check + boot-env testleri); hash artık **`v1:`+HMAC(`orgId|type:norm`)** — org HMAC domain'inde (tenant-korelasyon imkânsız) + versiyon prefix'i (rotasyon v2 olarak gelir, sessiz mismatch olmaz). (3) **m41 `expiresAt`** (nullable; null=org-ömrü [AVUKAT], dolmuşsa guard yok sayar — testli). (4) adlandırma düzeltildi: scrub = **silme (Yön. m.8, maskeleme)**, "anonimleştirme" (m.10) İDDİA EDİLMEZ — UI/yorumlar/metin; UI copy-pin testleri ("kalıcı olarak silinir (maskelenir)" + "en geç 30 gün içinde sonuçlandırılır (KVKK m.13)" + "anonimleştir" kelimesi YOK). Route **OWNER-ONLY** (manager 403 kırmızı-önce; sayfa koşulu da owner). Toplam **1442 test**, taze PG 00→41 sıfır-drift. **3. sertleştirme (Codex "yapısal değil" itirazı HAKLIYDI):** verify-pass ana güvence OLAMAZ (stale-guard sync verify'DAN SONRA da yazabilir) → ingress yazımları YAPISAL kilide alındı: hospitable-sync rezervasyon-TX + thread-TX ve iCal satır-TX'i provider verisini KİLİT DIŞINDA çekip DB yazımını erasure ile AYNI org-scoped `pg_advisory_xact_lock` altında yapar + guard'ı KİLİT İÇİNDE taze okur (`loadErasureGuard(org, tx)`) → iki-sıralama teoremi: sync önce yazdıysa erasure maskeler; erasure öncedeyse sync taze tombstone'u görüp YAZMAZ. Verify-pass artık yalnız EK emniyet. Yan kazanım: m38 no-loss garantisi YAPISAL oldu (thread importu tek atomik TX — partial-write imkânsız; eski `prisma.message.create` spy dikişleri `$transaction`-spy'a taşındı, tx client global delegate spy'ını görmez). Kırmızı-önce tam-zamanlama: erasure sync'in FETCH'i sırasında commit olur (mock içine enjekte) → in-lock guard yazımı engeller (Hospitable + iCal satır-silinmiş varyant). **FAIL-CLOSED (Codex 3. madde):** tombstone VAR + `ERASURE_HMAC_SECRET` YOK (flag kapatılmış olsa bile — secret KALDIRILAMAZ) → guard BLOCK_ALL, o org'un sync'i HİÇBİR ŞEY almaz (throw yok, reportError'lu; testli). **expiresAt şartı belgelendi:** "senkron geri getiremez" vaadi yalnız null-expiry'de mutlak; avukat süre seçerse flag o karara dek kapalı + süre ancak upstream-erişilemezlik garantisi ve UI metniyle BİRLİKTE kullanılabilir (draft §8c-6). Supply-tespiti TX-sonrasına taşındı (FK commit'te doğar). Toplam **1445 test**, 00→41 sıfır-drift, migration REWRITE YOK.

- 07-18 (4): **Codex 8-bulgu turu (2 P1-canlı + 6 P1/P2, m42 `42_erasure_tombstone_key_fingerprint`):** (P1 CANLI) şikâyet alert e-postası `users` sorgusu `role:"owner"` filtresizdi (`automation.ts` inline-escalation + `sendDueAlerts`) → staff-önce org'da misafir adı/gövde/mülk staff'a sızabilirdi (QR emsali); trial-reminders da aynı desende düzeltildi; kırmızı-önce staff-eski-owner testi. (P1) **erasure HMAC fingerprint** (`erasure.ts`): `v1:` yalnız ŞEMA sürümü — SECRET DEĞERİ değişince (rotasyon/yanlış-yazım) sessiz diriltme; her tombstone'a `keyFingerprint` (m42, HMAC-türevli, sızıntısız) yazılır, guard-load canlı satır fingerprint'i ≠ mevcut anahtar → BLOCK_ALL (fail-closed; kırmızı-önce yanlış-secret testi). (P1) **outbox PII**: executor `MessageOutbox.body` gönderim metnini de maskeler (bağlı reservation/conversation, org-scoped); teslim edilmemiş+claim'siz satır `canceled` (sentinel gitmez), terminal satır yalnız body-redakte. (P1) **UI karışıklığı**: `initialErased` artık `guestName===ANON_NAME` değil GERÇEK tombstone varlığı (`reservationsWithSourceTombstone`) — retention-maskeli satır hâlâ silme düğmesi gösterir. (P2) **audit dayanıklı**: erase route `writeAudit(...).catch(()=>{})` → try/catch + reportError (mecburi legal kayıt yutulmaz); yorum "opak reservationId, PII yok" netleşti. (P2) **erasedAt=max**: ikinci erasure `createMany(skipDuplicates)` + `updateMany(erasedAt<yeni → yeni)` — kişi anahtarları t2'ye ilerler (kırmızı-önce ikinci-stay testi). (P2) **yeni-konaklama sınırı ARRIVAL'a çekildi**: `blocksGuestStay` departure yerine arrival (arrival≤erasedAt → blok; overlapping/ongoing stay yeni sanılmaz; kırmızı-önce overlap testi). (P2) **body-cap authenticated sweep**: `readJsonCappedOrNull` (api.ts, `req.json()` sadık drop-in, stream cap) 37 auth JSON rotasına uygulandı (ai/test, reply, ai-suggest, settings, tasks, kb, hospitable/connect, billing, properties…). **1452 test**, taze PG 00→42 sıfır-drift, build temiz. VERBİS/İtalyan-şirket/18-altı = hukuk sorusu (avukat), kod dışı.
- 07-19: **Codex 7-bulgu turu (hepsi KOD-DOĞRULANDI; MIGRATION YOK, PUSH'landı) — 5 uygula + 2 gerekçeli-red + 1 ertele.** (P1-A YETKİ) billing UI owner-only ama API withManage'di → **manager doğrudan API ile plan değiştirebilir/portal açabilir/consent yazabilirdi**. Yeni `withOwner` HOF (route-guard.ts, withManage yanı) → portal/plan-preview/plan-change/consent 4 rotası owner-only; UI zaten manager'a billing göstermiyor → sıkılaştırma hiçbir akışı bozmaz, niyetle hizalar; erase rotası `withOwner`'a GEÇMEDİ (feature-gate `notFound()` role-gate'ten ÖNCE koşmalı — dormant→invisible korunur, inline owner-check kaldı). Kırmızı-önce manager→403 (3 rota). (P2-A DOĞRULAMA) `Number("")===0` → boş handoffHoldHours/otomasyon-saati sunucuda sessizce 0 kaydediyordu (client guard'ı direkt API çağrısı atlar; 0-saat hold = AI insan-devrinden HEMEN sonra devam). `toIntOrNaN` (number VEYA boş-olmayan sayısal string; gerisi NaN→400); manuel-stil korundu (zod yeniden-yazımı RED — rota tutarlılığı). Kırmızı-önce boş→400 / açık-0→200. (P2-C) trial cron e-postası CTA'sı `/settings`→`?tab=faturalandirma` (varsayılan görünüm AI oldu → çıplak /settings planları göstermiyordu). (P3 PERF/3P) settings-sections tüm panelleri mounted tutuyordu → owner AI'da olsa bile **billing paneli Paddle SDK'sını yüklüyordu**. Panel artık **aktif VEYA daha önce ziyaret edilmiş** olunca mount olur (Codex'in "önceden ziyaret edilmiş" önerisi; form-state korunur — ziyaret sonrası mounted kalır). `#hospitable` scroll'u panel-mount SONRASINA taşındı (ref+`[active,seen]` effect; lazy-mount'ta kart ilk gösterimde DOM'a girer → getElementById boş dönmesin). (P1-B kısmi) audit drop reportError context'i artık `audit.write:{action}` (hangi zorunlu legal kaydın düştüğü görünür). **GEREKÇELİ RED:** (P1-B atomik `writeAuditRequired`) audit hatası ZATEN writeAudit-içi reportError ile yüzeyleniyor; audit'i silme-TX'ine atomik bağlamak = geçici log hatası yasal-zorunlu SİLMEYİ bloklar (KVKK'da asıl zorunlu imha; log yeniden-üretilebilir) → daha kötü failure-mode. (P1-C legacy-foto durable queue) kodda+backlog'da zaten "S3/R2 bucket-açma turu"na ertelenmiş; Railway'de yerel disk ephemeral (kalıntı sonraki deploy'da silinir), hata reportError'la yüzeyli → ağır (yeni tablo), hotfix değil. **ERTELE:** (P2-B timezone display formatters) utils.ts+bu liste'de bilinçli-ertelenen görüntüleme katmanı; doğruluk katmanı zaten org.timezone; düzinelerce çağrı; tek gerçek müşteri İstanbul. **1487 test** (unit+ui 673 + integration 814), typecheck/lint/build temiz.
- 07-19 (2): **Codex geri-itiş turu — 3 noktada Codex HAKLIYDI, düzeltildi (MIGRATION YOK, PUSH'landı).** (P1-B ARTIK ATOMİK — önceki "gerekçeli red" GERİ ALINDI) reportError bir ALARM'dır, zorunlu AuditLog KAYDI değil; writeAudit hatayı içeride yuttuğu için erasure rotası audit'in yazılmadığını ÖĞRENEMİYOR + 200 dönüyordu; "sonradan yeniden üretilebilir" iddiasını dayandıran kalıcı repair kaydı YOKTU. Fix: `writeAuditInTx(tx, entry)` (audit.ts — TX-client alır, hata FIRLATIR = fire-and-forget writeAudit'in TERSİ, yalnız yasal-zorunlu atomik audit'ler için) → scrub+tombstone+AuditLog `eraseReservationData`'nın TEK transaction'ında commit olur; audit yazılamazsa TÜM erasure geri sarılır → route 500 (owner retry) → "loglamadan imha" (Silme Yön. m.7) YAPISAL olarak imkânsız. Metadata birebir korundu (sayı-only, PII yok) → route audit testi aynen geçer. Kırmızı-önce: (a) FK-ihlaliyle in-TX audit-insert patlatılır → reservation MASKELENMEZ + tombstone=0 (atomik rollback + rejects) · (b) in-TX audit success (actorUserId=owner, 1 satır, ad yok). "Blokla=daha kötü" eski gerekçem ZAYIFTI: aynı DB+aynı TX tek insert bağımsız-hata olasılığı ~sıfır; m.7 zaten imhanın loglanmasını ŞART koşuyor (loglamadan imha istenen başarı durumu değil); 30g SLA transient hatayı retry ile soğurur. writeAudit (login/impersonation/2FA/plan-change) fire-and-forget KALDI — orada audit hatası aksiyonu bloklamamalı. (P1-C legacy-foto) erteleme kabul AMA artık AÇIK PRIVACY maddesi (aşağı [infra/OPS]): ephemeral disk ANINDA-silme garantisi vermez → sonraki deploy'a dek `public/uploads/{org}` altında public dosya kalabilir; canlı legacy foto olmadığı AYRICA doğrulanmalı; durable temizlik-kuyruğu S3/R2 bucket turuna bağlı. (P2-B timezone display) bugün ertelendi ama "kozmetik" DEĞİL → yurt-dışı müşteri ÖNCESİ **operasyonel gereksinim** olarak kaydedildi (aşağıda güncellendi). (P3 testleri AYRIŞTIRILDI, Codex isteği) settings-sections: (1) mount-spy ile "billing ziyaret edilmeden Paddle mount side-effect'i KOŞMAZ" · (2) `?tab=faturalandirma`→initialViewId ilk-boyada billing mount · (3) `#hospitable` aktivasyon+scroll · (4) lazy-mount panelde form-state sekme değişince korunur — DÖRDÜ AYRI test. **1492 test**, typecheck/lint/build temiz.
- 07-19 (3): **10-mercekli denetim turu (kod-doğrulanmış; MIGRATION YOK, PUSH'landı).** Çekirdek (auth/RBAC 78 rota IDOR-temiz · billing hot-path · KVKK-isolation · outbox/sync tenant-izolasyon · concurrency para/erasure/auth) TEMİZ — sömürülebilir P1 yok. **7 gerçek fix uygulandı:** (P1 SEND — en kritik) `hospitableFetch` canlı POST'u 5xx/timeout/network'te `MAX_RETRIES` deniyordu → teslim-sonrası-yanıt-kayıp penceresinde misafire ÇİFT gönderim (outbox `retries:0` ile bunu önlüyordu ama canlı yol [DURABLE_OUTBOX default-OFF: manuel+auto-reply+holding+welcome/checkin/checkout] önlemiyordu; kod tehlikeyi "in-fetch duplicate window" diye adıyla anıyor). Fix: `sendOnChannel`→`sendMessage(...,{retries:0})` = outbox paritesi; caller'ın claim-then-send'i zaten ambiguous'u sahipleniyor (tut, kör-resend YOK) → MEVCUT dokümante invariant'la ("dup > silent miss") hizalar, GET'ler idempotent retry'li kalır. (P2 AI) `detectRiskType` `human_request`'i discrimination/rule_violation'dan ÖNCE döndürüyordu → "ev sahibiyle görüş + Suriyeli temizlikçi gönderme" gibi CO-OCCURRING mesaj human_request etiketlenip handoff-ack muafiyetiyle OTO-YANIT (tier-3 eskalasyon sessiz düşüyordu); fix: discrimination/rule_violation human_request'in ÜSTÜne → step-6 backstop eskale eder (golden: co-occurrence + handoff-denial pin + saf-human_request hâlâ akar). (P2 AI) `isPositiveFeedback` emoji'yi whitelist'ten ÖNCE sıyırıyordu → "harika 👎" övgü sanılıp nezaket-kapanışı oto-gidiyordu (model yok, kapı yok); `NEGATIVE_EMOJI` guard'ı (isClosingAck paritesi). (P2 KVKK) GLM/Akash gölge sınıflandırıcı 5 çağrıda yalnız `guestIdentifier` redakte ediyordu → placeholder-kimlikte rezervasyonun GERÇEK adı Akash'a ham gidiyordu; `reservationGuestName` de redakte (quality-audit paritesi; SHADOW_AI default-OFF → pilot öncesi). (P3) property saat regex `\d{2}:\d{2}` "99:99"/"45:00" kabul ediyordu → aralık `([01]\d|2[0-3]):[0-5]\d` (QR saat-kapısı + misafire-teklif korunur). (P3) `parseIcsDate` date-only "20260231"→3 Mart rollover (NaN değil → isNaN guard boş) → round-trip guard (csv makeDate emsali). (P3) `ReservationPinControl` generate/remove sonrası `router.refresh()` yok → yan bant sayacı bayat; eklendi. (P2 gözlemlenebilirlik) OAuth callback iç fault'u (encrypt/DB) dış-hata etiketiyle yutuyordu → `reportError` (PAT-connect paritesi). Kırmızı-önce her fix'e test. **1500 test**, typecheck/lint/build temiz. **ERTELENEN (kod-doğrulanmış, gerçek ama büyük/riskli/düşük-değer → gelecek tur):** (perf P2) inbox/tasks `findMany` take'siz → pagination (ürün kararı) · (perf P3) sync per-reservation task-skip yok, reports bellek-materyalizasyonu, welcome KB findFirst N+1 (cap'li) · (concurrency P3) task checklist RMW lost-update (iki editör), conversations/reservations-cleanup operatör-araçları sync-lock'suz · (billing P3) Paddle `paused`→`past_due`→14g grace (paused kasıtlı, dunning kart-hatası içindi; koşullu) · (AI P3) stated-time "leav" cue "leave AC until 22:00"u checkout sanıyor (düzeltme gerçek checkout tespitini kırma riski) · (KVKK P3) `redactNameFromBody` soyad-tek redakte etmiyor ("Sn. Yılmaz"; over-redaction riski) · (error P3) complaint-alert `send`→`sendReporting`, `summarizeHostStyle`/`chat[token]`/`reservations/import` reportError (additive gözlemlenebilirlik).
- 07-19 (4): **Codex #1 iki-teyit turu (kod-doğrulandı; MIGRATION YOK, PUSH'landı).** (teyit-1) `retries:0` TÜM canlı yollarda: 6 `sendOnChannel` çağrısı = manuel reply + holding-ack + closing + AI auto-reply + welcome/checkin/checkout, hepsi single-shot; `sendMessage`'ı doğrudan çağıran BAŞKA yol yok (yalnız sendOnChannel + outbox, ikisi de retries:0). (teyit-2 = GERÇEK BULGU) ambiguous→claim-tutulur her yolda testliydi (manuel `conversations-reply-route:202`, auto-reply, lifecycle, outbox `outbox-lifecycle`) — AMA **closing-courtesy istisnaydı:** `maybeSendClosingCourtesy` claim'i (`new/waiting→answered`) HERHANGİ hatada "new"e döndürüyordu (isDefinitiveSendFailure YOK) → ambiguous(5xx)'ta teslim-edilmişse sonraki pass re-claim → ÇİFT nezaket. Fix: closing de yalnız DEFINITIVE'de bırakır, ambiguous'ta claim TUTAR (thread zaten kapanıştı → "answered" uygun; single-shot #1 in-fetch dup'ı kaldırdı, bu caller-dup'ı kaldırır). holding-ack kontrol edildi: status revert etmiyor + thread zaten "problem" → re-send yolu yok, güvenli. Kırmızı-önce: mevcut "delivery failure" testi HTTP 500(ambiguous) ile ESKİ revert'i pinliyordu → claim-tutulur + ikinci-pass-sıfır-gönderim'e güncellendi + DEFINITIVE(4xx) release testi eklendi. **Ayrıca Codex'in "outbox founder-token cross-tenant" P0'ı Eski checkout'tan (2026-07-02) — mevcut kodda `worker.ts:517` guard'ı + `outbox.test.ts:153` regresyon testiyle ZATEN kapalı (07-16 (7)); değişiklik gerekmedi.** **1501 test**, typecheck/lint/build temiz.
- 07-19 (5): **Codex batch (eski 2026-07-02 checkout) — EN ÖNEMLİ 1 fix uygulandı (kod-doğrulandı; MIGRATION YOK, PUSH'landı).** (P1 MULTIPART OOM — uygulandı) `upload/route.ts` + `reservations/import/route.ts` `req.formData()`'yı boyut kontrolünden ÖNCE çağırıyordu → `file.size`/`file.text()` kontrolleri gövde ZATEN belleğe alındıktan sonra koşuyor → auth'lu bir kullanıcının yüzlerce-MB upload'ı paylaşımlı replica'yı OOM'lar. Fix: `contentLengthOverLimit(req, maxBytes)` (api.ts) = `req.formData()`'dan ÖNCE Content-Length header'ıyla erken 413 (upload 5MB+1MB, import 5MB+1MB envelope payı); gerçek tarayıcı/curl upload'u daima Content-Length gönderir = kapatılan OOM vektörü, eksik/yalan header için post-parse file.size ikinci hat. Kırmızı-önce: helper unit (over→413/under-absent-NaN→null) + import route entegrasyon (7MB header → 413, formData'dan ÖNCE; pre-check kaçsaydı 400 dönerdi). **DEĞERLENDİRİLEN, gerekçeli ERTELE (kod-doğrulandı):** (P1 email fail-open) register default-KAPALI (`REGISTRATION_OPEN`) → canlı değil; prod-boot mail-zorunluluğu + prod'da alıcı/gövde loglamama ayrı ops turu · (P2 HTTPS-pin) supply-ai/shadow-ai/hospitable base URL env'den http:// kabul edebilir AMA yanlış-env misconfig şart (default https); prod-boot https-zorunluluğu savunma-derinliği, ayrı tur · (P2 dep) nodemailer 8.0.11/postcss — sömürü yolu erişilemez, kontrollü bump · (P3) CSP report-uri yok · (P3) mobil drawer a11y (role/focus-trap) · (ölçek) export bellek-materyalizasyonu — büyük müşteri öncesi streaming. Kullanıcı "tek 1 tane en önemlisini" dedi → yalnız OOM. **1504 test**, typecheck/lint/build temiz.
- 07-19 (6): **P1 OOM tam kapanış (Codex geri-itişi HAKLIYDI; MIGRATION YOK, PUSH'landı).** İlk fix yalnız Content-Length header'ını kontrol ediyordu → **eksik/yalan CL veya chunked gövde hâlâ `req.formData()` içinde belleğe alınıyordu** (post-parse `file.size` OOM koruması DEĞİL). Doğru fix: `readFormDataCapped(req, maxBytes)` (api.ts) — (a) ucuz CL header ön-kontrolü + (b) **STREAMING byte-cap**: `req.body`'yi byte sayarak sarar, cap aşılınca kaynağı `reader.cancel()` ile İPTAL eder (geri kalanı belleğe çekmez) ve `BodyTooLargeError` fırlatır; byte-cap'li stream platformun KANITLI `Request.formData()` parser'ına verilir (elle multipart parser YOK — Codex'in busboy önerisinin amacı: hand-roll etme; platform parser'ı zaten proven). readTextCapped'in text/JSON için yaptığının multipart karşılığı. İki rota da `readFormDataCapped` + try/catch (BodyTooLarge→413, malformed→400); per-FILE `file.size` cap iş-kuralı olarak KALDI. Kırmızı-önce: chunked (CL YOK) >cap → BodyTooLargeError + kaynak CANCEL'lı + tamamı çekilmedi (pull sayısı ≪ tam-drain); yalan-küçük-CL + büyük gerçek stream → yine streaming cap yakalar + cancel; dürüst >cap CL → erken red; normal küçük multipart → doğru parse. **1506 test**, typecheck/lint/build temiz. **P1 artık gerçekten kapalı.**
- 07-19 (7): **Production e-posta FAIL-OPEN kapatıldı (Codex 6-madde; MIGRATION YOK, PUSH'landı).** (1 BOOT) `env-check.mjs`: prod'da **Resend VEYA eksiksiz SMTP (HOST+USER+PASS) ZORUNLU**; yarım SMTP = HATA (boot fail). Dev/test hiç bloklanmaz (verify-env yalnız prod'da exit-1, dev'de warn). **⚠️ DEPLOY ŞARTI: Railway'de `RESEND_API_KEY` veya eksiksiz SMTP OLMALI, yoksa sonraki deploy [bilinçli] boot-fail eder** — TRIAL_EMAILS_ENABLED=1 açık olduğundan RESEND büyük olasılıkla zaten var, yine de DEPLOY ÖNCESİ TEYİT. (2 email.ts) `send()` sağlayıcı yokken **üretimde To/Subject/Body/token'ı ASLA loglamaz** (secret-free `console.error`; DEV echo yalnız development/test); gönderim başarısızsa provider hatası `scrubForLog` (e-posta+uzun-sayı sıyırma; report-error'ı import EDEMEZ — cycle). (3 register) fire-and-forget `send()`→`sendReporting()` sonucu KONTROL: başarısızsa **503 (201 DEĞİL) + hesap KALIR** (verify token canlı→resend-able) + `reportError`; hesap SİLİNMEZ, email TX DIŞINDA (zaten öyleydi); kullanıcıya provider detayı sızmaz. (4 diğer yollar) resend-verification `send`→`sendReporting`+`reportError` (generik 200 KORUNUR=enumeration), forgot-password gönderim başarısızsa `reportError` (kod-temizliği+generik 200 zaten vardı), QR escalation `guest-chat-alerts` ZATEN doğru (sendReporting+releaseClaim+reportError). (5 kırmızı-önce) prod+provider-yok→boot fail · prod+yarım-SMTP→boot fail · dev+provider-yok→boot geçer · prod-fallback logunda recipient/body/token YOK · register send-fail→503+hesap+resend-able+provider-detay-sızmaz · başarı→201; boot-env test'i provider'lı güncellendi. **1514 test**, typecheck/lint/build temiz. **Sonra (CI):** e2e job production sunucusunu Playwright webServer ile başlatıyor → verify-env prod modda koşup yeni mail-kapısına takıldı (`efad1dd` CI kırmızı); fix `8065868` = `ci.yml` YALNIZ e2e job env'ine sahte gönderimsiz `RESEND_API_KEY: ci-e2e-provider-not-used` (prod kapısı/boot testleri/Railway Variables DOKUNULMADI); CI 4/4 yeşil + Railway `8065868` ACTIVE + gerçek prod e-postası uçtan-uca doğrulandı (`noreply@lixusai.com`).
- 07-20: **P2 HTTPS-pin turu (Codex; MIGRATION YOK, PUSH'landı) — 07-19 (5)'te ertelenen madde kapatıldı.** Secret taşıyan, env ile değiştirilebilen DIŞ servis URL'leri artık production'da yalnız https. **Kapsam (kod-haritalandı):** `HOSPITABLE_API_BASE_URL` (per-tenant Bearer), `SUPPLY_AI_BASE_URL` + `SHADOW_AI_BASE_URL` (Bearer AI key), `HOSPITABLE_OAUTH_AUTHORIZE_URL` + `HOSPITABLE_OAUTH_TOKEN_URL` (client_secret). DIŞARIDA (bilinçli): OpenAI (`ai/index`/`translate` hardcoded https, env-override YOK), Anthropic (SDK default, override YOK), Paddle (baseUrl env enum'dan türer, hardcoded https), `DATABASE_URL` + Railway iç adresleri (Codex şartı — credential-taşıyan dış HTTP endpoint DEĞİL). **İKİ KATMAN:** (1 BOOT) `env-check.mjs` (STORAGE_ENDPOINT emsali) — SET olan her URL prod'da https değilse boot-fail (yalnız SET olanı denetler, unset→kodun https default'u→sorun yok; yalnız alan ADI loglanır, değer ASLA; trafik başlamadan). (2 RUNTIME) `src/lib/secure-url.ts` `isSecureExternalUrl` TEK predikat = **prod: yalnız https; dev/test: https VEYA localhost-http** (enjekte test transport/yerel mock; non-localhost http dev/test'te DE reddedilir — carve-out dar). Bağlandığı yerler: supply-ai (insecure→`{ok:false,insecure_base_url}`, fetch YOK, key gitmez), shadow-ai (eski `startsWith("https://")` → helper, artık localhost-http dev'de serbest; http://api.akashml.com hâlâ pasif [non-localhost]), hospitable `hospitableFetch` (insecure→throw ÖNCE fetch, token gitmez), hospitable-oauth `postTokenRequest` (insecure→throw, authFailure:false=config sorunu, refresh token silinmez). URL/secret ASLA loglanmaz. **Kırmızı-önce:** secure-url unit (prod http red / dev localhost-http geçer / non-localhost http red / garbage fail-closed), boot-env checkProductionEnv+gate (prod http→exit1+URL-değeri-basmaz / prod https→exit0 / dev http→exit0), supply-ai (insecure→ağ çağrısı YOK). **1523 test**, typecheck/lint/build temiz. **Gerekçeli DIŞARIDA:** `APP_URL`/`HOSPITABLE_OAUTH_REDIRECT_URI` (kendi domainimiz, "dış servis" değil; APP_URL zaten `appBaseUrl()` allowlist+canonical-https default; token-sızıntı vektörü ama ayrı kategori → istenirse ayrı tur).

## ⏳ AÇIK İŞLER (tek birleşik liste)
**[denetim 07-17 ertelenen — kod-doğrulanmış, riskli/büyük → ayrı tur]** (1) ~~Sync `lastMessageAt` cursor foreign-write (HIGH, mesaj kaybı)~~ ✅ ÇÖZÜLDÜ (m38 `fbc5544`): skip-check cursor'ı AYRI `Conversation.syncCursorAt` kolonuna taşındı — SADECE message-sync yazar (provider `reservation.last_message_at`), outbound yollar HİÇ dokunmaz → now() kirliliği YAPISAL olarak imkansız. Codex 2 geri bildirimi haklıydı ve bu tasarım ikisini de çözer: (a) mesaj `created_at`'ine hiç bağlı değil (ilk fix'in guest-createdAt yaklaşımı `parseDate(created_at) ?? now()` ile kirlenebiliyordu), (b) değişmemiş thread'de cursor==incomingLast → skip KORUNUR (busy-account gereksiz fetch yok). null=hiç-senklenmedi→import (güvenli backfill). m38 taze PG 00→38 sıfır-drift + kırmızı-önce test. **Codex 3. delik (`28699d3`):** create-dalı syncCursorAt=createCursor yazıyordu → tek-mesajlı/aynı-zamanlı thread'de createCursor==incomingLast + mesaj-yazım throw'u (per-reservation yutulur) → sonraki sync skip → kayıp. Fix: create'te syncCursorAt NULL kalır, YALNIZ loop-sonu update (tüm mesajlar yazıldıktan sonra) doldurur; kırmızı-önce test (mid-loop throw). (2) ~~Outbox lifecycle welcome/checkin/checkout ambiguous rollback (P2)~~ ✅ ÇÖZÜLDÜ (`4a48c40`): tek-kaynak `isDefinitiveSendFailure(error)` (messaging.ts) — welcome/checkin/checkout + inline auto-reply + manuel-reply HEPSİ ambiguous(408/5xx/network)'ta claim TUTAR (kör-resend YOK → duplicate yok), definitive(4xx≠408)'te release+retry. auto-reply de aynı sınıftı (status-claim), o da bağlandı. 4 yol tek-kaynakta → drift imkansız; kapsamlı ambiguous/definitive testleri. (3) ~~hospitable-sync CREATE dalı scrub-check (P1 KVKK, DAR)~~ ✅ ÇÖZÜLDÜ (`84e74e7`, Commit A): CREATE dalı artık UPDATE'in `guestName===ANON_NAME` guard'ını AYNALIYOR — scrubbed rezervasyona (24ay+ anonimleşmiş) yeni thread gelirse `guestIdentifier=ANON_ID` + `scrubbedThread=true` yazılır, gerçek ad/gövde DİRİLTİLMEZ. Kırmızı-önce KVKK testi (conv YOK + full_name "Ada Lovelace" + pre-cutoff mesaj → guestIdentifier=ANON_ID, "Ada Lovelace" gövdede YOK, msgs=0). MIGRATION YOK. (4) ~~import/sync `catch {}` + `lastStatus` (P2)~~ ✅ ÇÖZÜLDÜ (`84e74e7` Commit A + `9fa657d` Codex sertleştirmesi): `catch {}`→`catch(err)` + `lastStatus` artık `errors.length ? "error" : "ok"` (1/100 geçse "ok" demiyor). **Sertleştirme (`9fa657d`):** ilk fix satır BAŞINA reportError çağırıyordu → e-posta context-başına 10dk throttle'lı ama her satır yine ayrı Sentry event + Railway log → büyük DB arızasında (tüm satırlar) yüzlerce/binlerce olay. Artık döngüde YALNIZ ilk hata örneği tutulur (`firstRowError`), döngü sonunda `errors.length>0` ise import çalışması başına TEK `reportError("import.sync", aggregate)` (mesaj yalnız "N satır kaydedilemedi — örnek hata: <sample>"; ad/gövde/satır verisi YOK, redactSensitive örnekteki PII'yi siler; sabit context → throttle çalışma-türüne göre). Kırmızı-önce: 100 satır HEPSİ patlar → reportError tam 1 kez + lastStatus="error" + mesaj "100 satır" içerir, "Guest" İÇERMEZ; partial-import testi de `toHaveBeenCalledTimes(1)`'e sıkılaştırıldı. MIGRATION YOK. (5) ~~Body-size cap (P2)~~ ✅ ÇÖZÜLDÜ (`1159630`+`ef31cd3`): `readTextCapped`/`readJsonCapped`/`parseJsonBody` (lib/api.ts, 64KB genel / 256KB webhook — Content-Length HEMEN reddi + Content-Length'siz/yalan gövdede streaming byte-cap; cap aşımında `reader.cancel()` ile stream GERÇEKTEN iptal, releaseLock değil; too-large→413, malformed→mevcut 400) TÜM unauth public POST'a bağlandı: demo/ai, leads, register, login, resend-verification, chat, **Paddle webhook** (raw-text imza korunur — okuma imzadan ÖNCE), **account/forgot-password** (Codex — atlanmıştı). Authenticated=auth+rate-limit. (6) ~~hospitable OAuth refresh TOCTOU (P2)~~ ✅ ÇÖZÜLDÜ (`ac0553c`, Commit B): findUnique-sonra-clear (check-then-act) → koşullu `updateMany({where:{id,hospitableRefreshTokenEnc:failedToken}})` — kazanan taze token'ı rotasyona soktuğu an 0 satır eşleşir → clear no-op (okuma-sonra-act penceresi yok); authFailure değilse (transient 5xx/network) davranış aynen. Kırmızı-önce yarış testi (kaybeden mid-flight kazananın "access-WINNER"ını persist eder sonra invalid_grant → connected=true + token hayatta; unconditional clear sağlıklı bağlantıyı koparırdı). MIGRATION YOK. ⚠️ Not: gerçek async TOCTOU penceresi deterministik reprodüksiyona uygun DEĞİL (fix'li=tek updateMany vs fix'siz=findUnique+clear ayrı sorgu-yolları) → test atomik-koşullu davranışın REGRESYON-PİNİ. (7) **ics.ts (P2/P3) — GEREKÇELİ ATLANDI (kullanıcı onaylı, kod-doğrulandı):** geçersiz tarih zaten savunuluyor (`parseIcsDate` fallback `isNaN`→null→skip + tüketici `sync.ts` `isNaN`+tarih-sırası kontrolü); TZID drop yalnız SAATLİ event'i etkiler ama Airbnb/Booking feed'leri tarih-only (`VALUE=DATE`) → pratik etki YOK; foldLine yalnız .ics EXPORT'unda 75. sütun civarı çok-baytlı ad → kozmetik + misafir adı feed'de default gizli. Kazanç yok → ayrı tur açılmadı. (8) ~~login timing enumeration (P3)~~ ✅ ÇÖZÜLDÜ (`d480e4d`): user yoksa artık sabit `DUMMY_HASH`'e karşı `dummyVerifyPassword` koşar → yanıt gecikmesi kayıtlı e-postayı ele vermez; kırmızı-önce (bilinmeyen e-posta→dummy 1×/verifyPassword 0×; bilinen→tersi). MIGRATION YOK. (9) ~~trusted-device HS256 pin~~ ✅ (`28baf31`: `algorithms:["HS256"]`, HS512-forge reddi — session.ts ile parite) · ~~a11y label'ları~~ ✅ (`a5bd6aa`: delete-account şifre + calendar-sources 2 input + ai-test select aria-label; conversation-thread "Ton" ZATEN etiketliydi → dokunulmadı) · ~~QR mustEscalate riskType paritesi~~ ✅ (`f63d355`: `HIGH_STAKES_RISK_TYPES` automation.ts'ten export→TEK kaynak, QR gate de model etiketine göre eskale; kapıda handoff-ack muafiyeti YOK; golden 105 nötr) · ~~N+1 importThread + `@@index([chatToken])`~~ ✅ (`ccf42e4` batched Set [findFirst 3→0] + m39 `726fd00` index). **KALAN (ertelendi):** outbox review→pending yolu/attemptCount decrement/rate-count exclude (P3 — durable-outbox flag default-KAPALI → flag açılış turunda). **[izle]** `isGuestMessage` alan-adı gerçek Hospitable payload'la (yanlışsa tüm inbound sessizce durur).

**[migration — kalan]** para Faz-B/C (Dec'i tek kaynak yap → Float'ı düşür; Faz-B ÖNKOŞULU SAĞLANDI: prod reconciliation=0 CANLIDA doğrulandı 2026-07-13; ~~Faz-A~~ ✅ m23 deploy+backfill canlı) (~~RiskEvent~~ ✅ m22) · **Message.authorType/systemEventType Faz-B (m28 sonrası):** Faz-A=dual-write+backfill+KALICI deriveMessageAuthor fallback (rolling-deploy overlap NULL'ları okumada güvenli; healing=idempotent backfill re-run, 2-dk tarama YOK). Faz-B (authorType NOT NULL / fallback düşür) AYRI migration + ÖNKOŞUL prod `count(Message WHERE authorType IS NULL)=0`. senderName artık yalnız görüntüleme/audit; reports/sent/ui-labels authorType-öncelikli (para semantiği korundu, QR "chat" hariç). · Task `@@unique([propertyId,dedupeKey])` bilinçli DIŞARIDA (semantik: done→aynı-gün yeniden açılabilmeli). ~~conversationId+externalId · propertyId+sourceReference · Subscription provider+providerRef · verify-hash index · Task.origin~~ ✅ m17-21.
**[infra/OPS — bucket açma turu]** StorageObject manifesti (#3, Codex 07-16): upload'lanan foto önce bucket'a yazılıp AYRI PATCH ile göreve bağlanıyor — tarayıcı kapanır/PATCH başarısız olursa DB'den erişilemeyen orphan obje kalır (hesap silme yalnız TaskUpdate'ten bulduğu anahtarları sildiği için KVKK silmeden de kaçar). Fix: kalıcı StorageObject tablosu + pending→attached→deletion_pending yaşam döngüsü + eski pending süpürme. GERÇEK gap ama ağır (yeni tablo+lifecycle); STORAGE_ENABLED default-OFF + bucket YOK → bucket açarken yapılacak, hotfix değil. **[AÇIK PRIVACY — Codex 07-19]** hesap silmede legacy `public/uploads/{org}` temizliği hata verirse yalnız reportError atılıp silme yine 200 dönüyor → dosya public diskte kalabilir; "ephemeral disk sonraki deploy'da siler" ANINDA-silme GARANTİSİ değil (deploy'a dek pencere var). Erteleme kabul çünkü (a) canlı legacy foto olmadığı doğrulanınca risk ~sıfır [DOĞRULA], (b) durable temizlik-kuyruğu zaten bu bucket turuna ait; ama backlog'da AÇIK privacy maddesi olarak kalır (sessizce kapatma).
**[infra/OPS]** ~~S3/R2 foto (upload sahiplik-bağı + signed URL; local /uploads ephemeral)~~ ✅ TEMEL HAZIR (m32, flag `STORAGE_ENABLED` DEFAULT KAPALI — bucket YOK, kod hazır+testli fake-adapterle; kalan: bucket aç + env + BİRLİKTE ilk-upload doğrula, opsiyonel legacy backfill AYRI tur/[KARAR]) · **durable outbox #8 (m29, flag `DURABLE_OUTBOX_ENABLED` DEFAULT KAPALI):** çekirdek+entegrasyon+Faz-B TAMAM (state machine, FOR UPDATE SKIP LOCKED claim, per-conv FIFO+tek-ucus, bounded backoff+jitter, ambiguous→kör-resend-YOK→review, worker flag-KAPALIYKEN-DE-drain #1, teslimde-answered #6, tenant-izolasyon #7, kalıcı UI rozeti #3). Bağlı yollar (m30 ile TAM): **manuel reply + AI oto-yanıt + holding-ack + welcome/checkin/checkout** (güvenlik kapısından SONRA enqueue; QR iç-mesaj HARİÇ) — ayrıntı Durum bölümünde. Kalan tek iş: flag'i AÇMAK = para hot-path → İLK gönderimleri BİRLİKTE doğrula. Ölçek notu: durable outbox artık VAR; claim-then-send flag-OFF yolu birebir korunur · ~~dağıtık rate-limit~~ ✅ m35 07-16 (DB-atomik ON CONFLICT sayaç, DB saati UTC; DB hatasında bellek fallback; sweep scheduled-sync'te; 38 call-site async — ayrıntı Tarihçe) · ~~ESLint'i minimal config'le geri aç + CI lint adımı~~ ✅ 07-18 (flat config `eslint.config.mjs`, `next/core-web-vitals`+`next/typescript`; 35 pre-existing violation kod-doğrulanarak düzeltildi — bazıları gerçek bug'dı: `deadline`/`clock` prefer-const'ta biri SSRF-timeout değişkeniydi [yanlış fix TypeScript'i kırardı, düzeltildi], `STATE_MAX_AGE` dead-const→OAuth cookie'sinin `maxAge:600` magic-number'ına bağlandı [tek-kaynak], `orgName` dead-param + kullanılmayan `organization.findUnique` sorgusu kaldırıldı; CI'a `npm run lint` adımı eklendi) · ~~rapor/otomasyon org.timezone~~ ✅ 07-16 (doğruluk katmanı TAM: gün sınırları/saat kapıları/rapor kovaları/QR açık-saat + Ayarlar seçici; KALAN: utils.ts APP_TIME_ZONE görüntüleme damgaları bilinçli Istanbul — ayrı görüntüleme-katmanı turu; **NOT (Codex 07-19): "kozmetik" DEĞİL → yurt-dışı müşteri ÖNCESİ operasyonel gereksinim** [NY host'un görev/inbox/QR zaman etiketleri İstanbul saatiyle yanlış görünür]; tek gerçek müşteri İstanbul'da olduğu için bugün ertelendi, ilk yabancı-tz müşteriden önce formatter'lara org.timezone taşınmalı) · per-org sync kuyruğu (global kilit tenant'ları seri bağlıyor) · ~~iCal DNS-rebind dispatcher~~ ✅ (node:https + validating lookup, undici'siz) + feed-URL at-rest şifreleme (UI maskeleme YAPILDI ✓) · Railway "Wait for CI" + backup/PITR (panel).
**[ürün kararı]** ~~QR per-misafir credential (booking-kanalı PIN)~~ ✅ FAZ 5 (#14: rezervasyona özel PIN, HMAC+pepper, opt-in default OFF `QR_PIN_ENABLED`) · ~~QR escalation e-postası~~ ✅ FAZ 3 (#15, env-gated default OFF `QR_ESCALATION_EMAIL_ENABLED`) · ~~2FA recovery kodları~~ ✅ FAZ 4 (#20, m26) · **QR PIN tek-tık kanal gönderimi (BACKLOG — Hospitable aktifken ilk gerçek gönderimi BİRLİKTE test ederek aç):** host'un kodu misafire kopyalamadan Airbnb dizisine göndermesi; ŞART: yalnız kanal-bağlı rezervasyonlar (externalConversationId var), host önizleme+onayı, idempotent claim-then-send, çift-gönderim koruması, başarısızlıkta PIN'i kaybetmeme, kanal-bağı yoksa/başarısızsa kopyalama fallback'i. Bugün: kopyala-taslak akışı (her rezervasyonda çalışır, gönderim hot-path'ine dokunmaz). · feed-disappearance reconcile (lastSeenAt/missingCount tasarımı) · ~~export'u gerçekten TAMAMLAMA~~ ✅ (#34 TAM: fatura/abonelik + audit + consent + görev-güncellemeleri/foto-linkleri + takvim kaynakları + supply + AI metadata + RiskEvent + ayarlar; secret-tarama pin testli; metin genişletildi) · ~~sekmeli Ayarlar~~ ✅ 07-18 (v1: 3-mercek deliberasyon [IA + Next.js mimari + Türk host]) → **v2 (kullanıcı/Codex revizyonu, aynı gün): yatay tab-strip KALDIRILDI** (desktop'ta çirkin yatay scrollbar; ekranla doğrulandı: `docScrollW==clientW` 1440+390'da). Yeni IA: **3 üst bölüm** — İşletme Ayarları(org-scoped; alt-görünümler: Yapay Zekâ[varsayılan]/Otomatik Mesajlar/Bağlantılar/Bildirimler/Saat Dilimi) · Faturalandırma(OWNER-only nav görünürlüğü; API withManage kalır) · Hesap ve Güvenlik(kullanıcıya özel: hesap+şifre, 2FA, sonra owner-only KVKK+Hesabı Sil tehlikeli bölge). Layout: **desktop sol kompakt sticky nav + ~900px içerik** (shell max-w-6xl tam kullanılır, kartların max-w-2xl'i kaldırıldı), **mobil native `<select>` bölüm seçici** (erişilebilir, optgroup İşletme). Büyük yeşil ana-şalter banner → **kompakt tek-satır durum çubuğu** (operatör-only; `AUTO_REPLY_ENABLED` env → toggle DEĞİL, durum göstergesi). Mimari korundu: server tek veri geçişi + tüm görünümleri render, client `SettingsSections` görünürlük değiştirir (inactive `hidden` → form girdisi korunur), görünür-set server'da ROLCE süzülür (boş bölüm imkânsız). Deep-link: `?hospitable=<kod>`+`?tab=<slug>`→`deriveInitialViewId` (SSR), `#hospitable`→mount effect (Bağlantılar+scroll; iki Hospitable dalında da `id`). İş mantığı/migration DOKUNULMADI. `settings-tabs.*`→`settings-nav.ts`+`settings-sections.tsx` (v1 kaldırıldı). **v3 (aynı gün, kullanıcı/Codex cila):** İşletme alt-nav 5→3 (AI ve Otomasyon[Yapay Zekâ+Otomatik Mesajlar] · Bağlantılar · Genel[Bildirimler+Saat Dilimi] — işlev silinmedi, aynı sayfada birleşti); sol nav kontrastı yükseltildi (aktif=primary-bg+sol çubuk+primary metin, pasif=foreground/70); içerik desktop'ta `max-w-7xl`; **sidebar kullanıcı kartı** artık plan gösteriyor (owner-only) + `/billing`'e link (yeni redirect route→`/settings?tab=faturalandirma`, robots-disallow'a eklendi) + kart alta YAPISAL sabit (nav tek scroll min-h-0, kart shrink-0 footer — yukarı kaçma bug'ı bitti); **Kaydet düğmeleri dirty-gated** (7 form: baseline+dirty, değişiklik yokken disabled, save sonrası baseline=current). Desktop+mobil ekran görüntüsüyle doğrulandı (h-scroll yok 1440+390; 6 Kaydet başta disabled). **v3.1 (kullanıcı cila):** iki kart taşındı (Toplu Check-in/out → AI ve Otomasyon [AI yanıtlarını besler]; Takvim Akışı Gizliliği → Genel), Bağlantılar'da yalnız Hospitable kaldı; **sidebar "havada duruyor" fix'i:** panel `zoom:.95`'i `100vh`'yi de %95'e küçültüyordu → sidebar/main alta ~5vh eksik kalıyordu; tam-yükseklik öğeler artık `calc(100vh/0.95)` (zoom sonrası tam 100vh; asideBottom==innerH 900/900 doğrulandı). **v3.2 (kullanıcı):** bulk check-in/out serbest-metin→`type="time"` (native saat seçici; `:00` elle yazma derdi bitti; mülk formu emsali; validator "HH:MM" aynı). Onboarding `#hospitable` deep-link'i gerçek tarayıcıda uçtan-uca doğrulandı (CTA→Ayarlar › Bağlantılar aktif + kart görünür, rework bozmadı). **v3.3 (6-ajan debugging turu, kod-doğrulanmış — 4 mercek TEMİZ):** 7 bulgu düzeltildi: (1) **en önemli** — Otomasyon Tercihleri'nde bekleme-süresi kutusu boşaltılınca sessizce **0-saat hold** kaydediliyordu (`Number("")=0`, 0-72 kontrolünden geçer → AI insan-devir talebinden hemen sonra devam) → client boş-kutu reddi, açık "0" hâlâ geçerli; (2) bulk-times rotası başında-sıfırsız `9:30` saklıyordu → `type="time"` boş render + yeniden-kaydedilemez → rota+form zero-pad; (3) alert-email/(4) ai-signature+closing/offer trim/lowercase sunucu-normalizasyonu client baseline'a yansımıyordu → hayalet-dirty + görünen≠kayıtlı → post-save state sunucuyla eşitlendi; (5) timezone-form kayıttan sonra `router.refresh()` yapmıyordu (kardeşleri yapıyor) → bayat gün-sınırları. + fatura linkleri: Paddle successUrl + trial/limited banner'lar düz `/settings` yerine `?tab=faturalandirma` (rework sonrası varsayılan AI görünümü plan kartlarını gizliyordu). Kırmızı-önce: bulk-times zero-pad route testi + boş-hold-hours guard UI testi. Unit+UI testli · ~~Claude kalite denetçisi v1~~ ✅ 07-16 (operatör paneli kartı, salt-okuma gölge — ayrıntı Tarihçe; nightly-cron + Batch API v2'ye bırakıldı; geniş müşteri verisinde kullanım öncesi DPA/KVKK aktarım kararı HÂLÂ ŞART) · ~~GLM/Akash gölge Aşama-1~~ ✅ 07-16 (m36 ShadowVerdict + shadow-ai.ts, flag `SHADOW_AI_ENABLED` DEFAULT KAPALI — ayrıntı Tarihçe; kullanıcı açar: anahtar/URL mevcut SUPPLY_AI_* değerlerinden devralınır, ilk 200 kayıtta otomatik durur, /admin "GLM Gölge Pilotu" kartı) → **Aşama-2 (İNSAN işi):** pilot dolunca ayrışma satırları birlikte değerlendirilir (risk-kaçırma vs yanlış-alarm) → **Aşama-3 (AYRI TUR, ancak Aşama-2 güven verirse):** GLM yalnız AÇIK güvenlik ihlalinde gönderimi DURDURabilir (veto-only; asla gönderim ONAYLAYAMAZ — kapıyı gevşetemez, sadece sıkabilir); Claude haftalık üst denetçi kalır. · opsiyonel liste: haftalık ops-özeti · review-isteme · lost-item · misafir dil/konu analitiği · temizlikçi paylaşım linki.
**[düşük]** ~~CSV tırnak-içi-newline parser~~ ✅ (RFC 4180 state-machine, csv.ts; yapısal bozuk→CsvParseError fail-closed 400, satır-eksik→skip; 31/02 rollover reddi; delimiter header'dan; dep YOK) · ~~yanıt-süresi episode-bazlı~~ ✅ (response-episodes.ts; aktivite-scoped + koşu-bazlı, İLK mesaja çapalı) · ~~#46 legalTextHash~~ ✅ 07-18 (m43 `legalTextHash`/`acceptedLegalTextHash`, nullable, additive): sha256 SECTIONS içeriğinden OTOMATİK türetilir (`src/lib/legal-text-hash.ts`; 4 yasal sayfanın `content.ts`'e taşınmış SECTIONS'ı hash'lenir) — LEGAL_VERSION gibi elle bump edilmez, metin değişirse hash kendiliğinden değişir (bump unutulsa bile kurcalama yakalanır). Kayıt (signup) + checkout consent + KVKK export'a bağlandı. 00→43 sıfır-drift. · ~~OG/Twitter görseli (asset)~~ ✅ zaten mevcuttu (bu tur fark edildi — `opengraph-image.tsx`+`twitter-image.tsx` gerçek 1200x630 `next/og` görseli üretiyor; bu liste bayatmış).
**[izle — canlıda doğrula]** `GUEST_ERASURE_ENABLED=1` açılışı BİRLİKTE + avukat imzası SONRASI (ilk gerçek silmede: **ÖNCE pg_dump yedeği** → önizleme sayıları doğru → onay → satırlar maskeli + tombstone yazıldı → yeniden sync tetikle → PII'nin DÖNMEDİĞİ doğrulanır) · AUTO_REPLY ilk gerçek gönderimler + thread'de duplicate yok · **DURABLE_OUTBOX_ENABLED=1 açılışı BİRLİKTE (manuel+AI oto-yanıt outbox'a geçer): ilk gönderimde MessageOutbox pending→sent + Message.externalId dolar + thread rozeti Sırada→İletildi + duplicate yok; review/failed satırı = takılı, elle incele) · safety_emergency false-hold oranı (BİLİNÇLİ AŞIRI-KAPSAYICI: "yangın merdiveni nerede?" bile insana düşer — çok olursa "acil+risk sinyali" kombinasyonuna incelt) · toggle açık org'da smart-task/supply +1 over/under-create · healthcheck'li ilk deploy'lar (Attempt#→Active) · `isGuestMessage` alan-adını gerçek Hospitable payload'la teyit.
**[LEGAL/kullanıcı — kod değil]** **Misafir AÇIK-SİLME talebi (m.11) + ErasureTombstone (araştırma turu 07-18, resmî kaynak doğrulanmış):** bugün misafir-düzeyi silme yüzeyi YOK (yalnız hesap-düzeyi + süre-bazlı anon). Hukuki zemin net: talep 30 günde sonuçlanır (m.13/Yön. m.12), işleme şartı sürüyorsa gerekçeli red MEŞRU (Kurul 2021/1104 emsali — banka 10y saklama), silinen veri "tekrar kullanılamaz" olmalı (Yön. m.8) → sync/iCal/QR ingress'ine per-talep tombstone (hash-anahtarlı, erasedAt cutoff) tasarımı `docs/DATA-RETENTION-ERASURE-DRAFT.md §8`. **İKİ AÇIK KARAR DA ARAŞTIRMAYLA DOLDURULDU (07-18/2. faz, resmî mevzuat):** (1) istisna kapsamı → Invoice **10y** (TTK m.82; VUK m.253=5y, sıkı olan) · CheckoutConsent **zorunlu ≥3y** (Mesafeli Söz. Yön. m.20/1, ispat yükü satıcıda) + **önerilen 10y** (TBK m.146 + m.5/2-e) · e-ileti onay/ret **3y** (Ticari İletişim Yön. m.13); (2) "erasedAt-sonrası yeni veri bloklanmaz" sınırı → m.7/1 imha MEVCUT veriye; sonraki konaklama/mesaj AYRI işleme faaliyeti (m.5/2-c,f; GDPR 17/3 paraleli); tombstone'un kendi dayanağı m.5/2-ç + İYS ret-kaydı emsali (ret kaydını saklamak bizzat zorunlu). → Açık [HUKUK KARARI] kalmadı; yalnız **[AVUKAT İMZASI]** (öneri setinin onayı). **m40 KODU YAZILDI (07-18/3. faz, kullanıcı "başla" dedi):** ErasureTombstone + guard'lar + host yüzeyi SHIPPED — yüzey `GUEST_ERASURE_ENABLED` default KAPALI, ingress guard'ları HER ZAMAN açık (boş tablo=no-op); avukat imzası FLAG AÇILMADAN önce alınır, kod beklemez · ~~SELLER `legal-entity.ts` [parantez]~~ ✅ P.IVA+telefon gerçek değerlerle dolduruldu (07-18) · **AI ALT-İŞLEYEN AÇIKLAMASI (gizlilik/page.tsx alt-işleyen listesi ŞU AN yalnız OpenAI'yi sayıyor):** artık 3 AI veri işleyen var → gizlilik metnine **Akash/GLM (gölge sınıflandırıcı, redakte veri)** + **Anthropic/Claude (kalite denetçisi, redakte veri)** eklenmeli; her biri için DPA + KVKK m.9 yurt-dışı aktarım güvencesi. ⚠️ Kod bu AI'ları redakte veriyle çağırıyor ama alt-işleyen listesi + DPA HUKUK işi (metni avukat yazar; kod otomatik eklemez — bilinçli). Aciliyet: ikisi de default-OFF/operatör-kontrollü + tek gerçek müşteri (Nuve=founder=veri sorumlusu) → geniş çok-kiracılı açılıştan ÖNCE ŞART · ~~KVKK-DPA: OpenAI ABD aktarımı → DPA~~ ✅ **OpenAI DPA İMZALANDI (07-18, DocuSign, Ironclad self-servis form):** Customer=Zeynep Cinar (Owner), adres Milano/İtalya → DPA otomatik **OpenAI Ireland Ltd.** ile kuruldu (OpenAI OpCo LLC/ABD DEĞİL — AB-tabanlı olduğu için doğru taraf); Org ID mevcut "Nuve" API hesabıyla eşleşiyor VE bu, ürünün canlıda kullandığı AYNI hesap (kullanıcı teyit etti — API key değişikliği gerekmedi; hesap sahipliği/ödeme abide kalabilir, DPA'daki hukuki sorumlu ablan olarak tanımlı, çelişki değil). "Company's full legal name" alanına şahıs-şirketi sahibinin adı yazıldı (Ad-Soyad formatı — LTD/AŞ'siz şahıs şirketlerinde geçerli format, ama gerçek vergi-levhası unvanıyla eşleşmesi ayrıca teyit edilmeli). Standart Sözleşme/Kurul-5-iş-günü maddesi hâlâ + host DPA + VERBİS · RESEND domain + SPF/DKIM/DMARC · saklama/erasure politika belgesi (Invoice/Audit cascade vs webhook iskeleti tutarlılığı) · ~~Paddle küçük gerçek ödeme birlikte-test~~ ✅ (07-18: Başlangıç gerçek kartla satın alındı → in-app upgrade ile Pro'ya geçildi, Paddle vendor dashboard MRR/revenue/subscriber sayılarıyla doğrulandı, ↑MEVCUT CANLI DURUM).

## Durum
**1523 test yeşil · typecheck temiz · lint temiz (CI'a bağlandı) · next build temiz · migrate deploy canlıda doğrulanmış · CI 4/4 yeşil.** 44 migration (00_init→43_legal_text_hash) sıfır-drift taze PG'de doğrulanmış (m43 = `legalTextHash`/`acceptedLegalTextHash`, nullable — consent metninin SECTIONS içeriğinden otomatik türeyen sha256, LEGAL_VERSION'ın kurcalama-kanıtlı eşlikçisi) (m38 = sync mesaj-kaybı `syncCursorAt`; m39 = `@@index([chatToken])`; m40+m41+m42 = KVKK ErasureTombstone + expiry + key-fingerprint — guard'lar HER ZAMAN açık [boş tablo=no-op], host yüzeyi `GUEST_ERASURE_ENABLED` DEFAULT KAPALI + OWNER-only, flag açılışı [AVUKAT İMZASI] + Railway'e `ERASURE_HMAC_SECRET` ekledikten sonra [dedicated, ≥32, ASLA rotasyon]). Branch `claude/great-edison-3zqpZ` origin ile SENKRON. Railway healthcheck-gated deploy AKTİF. Billing "tamam". **Durable Outbox #8 TAM kapsam (m30): manuel + AI oto-yanıt + holding-ack + welcome/checkin/checkout hepsi bağlı** (flag `DURABLE_OUTBOX_ENABLED` DEFAULT KAPALI; QR iç HARİÇ; *SentAt yalnız DOĞRULANMIŞ teslimde; lifecycle veto; **Hospitable 402 = kalıcı `blocked` "abonelik pasif"** [CLAIM edilmez, attempt tüketmez, her pass provider/pager YOK, sync-başarısında `reactivateBlockedOutbox` ile bir-kez pending; geçici outage DEĞİL → diriltme YOK]; rollback-güvenliği sahte SentAt DEĞİL flag-OFF sender'ın outbox kaydına FENCE'i (`lifecycleOutboxOwns` status not:failed→blocked'i kapsar)). **Outbox ops ekranı `/sent/queue`** (owner/manager; PII'siz liste — body/idempotencyKey/claimedBy asla; yalnız failed[≠402] satırına tenant-bound retry; blocked/review salt-okuma açıklamalı; flag OFF'ken geçmiş görünür). **#23 iCal feed-disappearance (m31, flag `ICAL_DISAPPEARANCE_RECONCILE_ENABLED` DEFAULT KAPALI):** 2 ardışık miss + 24h + suspicious/empty-skip + source-binding + per-source lock + stale-ordering, DELETE yok. #35 veri-envanteri TASLAK (`docs/DATA-RETENTION-ERASURE-DRAFT.md`). Kalan işler yukarıdaki AÇIK İŞLER listesinde — çekirdek defalarca tarandı, yeni tur açmadan önce oradan seç.
