# Denetim turu — 2026-08-09 (7 salt-okuma ajanı)

Kullanıcı direktifi: *"7 tane agent sal analiz kontrol cybersecurtilik yapsınlar derin uzun uzun."*
Ajanlar **yalnız araştırdı ve ölçtü**; her düzeltmeyi Claude yazdı (08-08 kuralı).
Mercekler: kimlik/oturum · çok-kiracılı izolasyon · AI saldırı yüzeyi · para yolu ·
KVKK/PII · altyapı-tedarik zinciri-SSRF · boş test avı.

> **Bu dosya bir KUSUR LİSTESİ DEĞİL, bir KARAR LİSTESİDİR.** Kapatılanlar
> "KAPANDI" damgalıdır (CLAUDE.md redaksiyon kuralı: dış okuyucu bunu canlı
> açık sanmasın). Açık kalanların her birinde **neden kapatılmadığı** yazılıdır.

---

## ✅ KAPANDI — bu turda uygulandı (hepsi kod-doğrulandı + mutasyon-pinli)

| # | Bulgu | Commit |
|---|---|---|
| 1 | **TOTP kodu `disable`/`recovery_codes`'ta yakılmıyordu** — "replay-korumalı" iddiası yalnız giriş için doğruydu; ±1 pencerede ~89 sn yaşayan bir kod, girişte kullanıldıktan SONRA ikinci kez faktörü silmek ya da 10 kalıcı bypass üretmek için kullanılabiliyordu | `032a0ef` |
| 2 | **`enable` koşulsuz yazıyordu** — zaten açık hesapta `twoFactorEnabledAt`i tazeleyip hatırlanan tüm cihazları düşürüyor + kurtarma kodlarını siliyordu; `disable` ile yarışınca kurtarılamaz duruma sokabiliyordu | `032a0ef` |
| 3 | **Çıkış `guestops_trusted_device` çerezini temizlemiyordu** — 2FA'yı ATLAYAN 30 günlük kimlik bilgisi çıkıştan sonra da tarayıcıda kalıyordu | `032a0ef` |
| 4 | **Kurucu muafiyeti mülk kapılarında yoktu** — `limitsForOrg` 07-31'de düzeltilmiş, `canAddProperty`/`resolvePropertyLimitState` atlanmıştı; kurucu ~10 daire işletirken Pro limiti 7 | `032a0ef` |
| 5 | **Stil profili botun kendi yanıtlarını örnekliyordu** — filtre yalnız `"GuestOps AI"` adını tanıyordu, QR concierge `"Lixus AI"` yazıyor → bot çıktısı "host sesi" sayılıp org geneli prompt'a CEVAP KAYNAĞI olarak dönüyordu | `6201d4e` |
| 6 | **Rezervasyon-öncesi KB kapısı tek bacaklıydı** (dün benim yazdığım kapı) — QR yolu kategori + içerik ikilisi uygularken oto-gönderen yüzey yalnız içerik bacağını taşıyordu | `6201d4e` |
| 7 | **QR kapısının deterministik vetosu 3 etiketle sınırlıydı** — inbox 08-06'da tam kümeye geçmiş, QR güncellenmemişti; "IBAN'ınızı atar mısınız" halka açık botta CEVAP ALIYORDU | `08ec175` |

### Kendi hatalarım (bu turda yakalandı, yazılı kalsın)
- **`trusted-device-login-wiring.test.ts` BOŞ TESTTİ.** `if (setTrusted.mock.calls.length > 0) … else …`
  sarmalı yazma yeri hiç çalışmasa bir üstteki testin iddiasını tekrarlayıp yeşil kalıyordu → S2'nin
  YAZMA tarafı fiilen pinsizdi. *"Emin değilim, ihtiyaten sarayım"* refleksi tam olarak boş test üretir.
- **Kategori bacağının kaynak-tarama pini `IMPORT SATIRIYLA` tatmin oluyordu** — bacağı silen mutasyon
  YEŞİL geçti (ölçüldü). Tarama artık `kbVisible` ifadesine çapalı. *Kaynak taraması ancak baktığı yer
  doğru olduğunda bir şey pinler.*

---

## 🔴 AÇIK — karar/onay bekliyor

### A. Para yolu (hepsi kullanıcı onayı ister — CLAUDE.md: *"para akışına dokunan şeyler kullanıcı onayıyla"*)

1. **Geçersiz webhook imzası SESSİZ.** `webhooks/paddle/route.ts:447` 401 döner, `reportError` YOK
   (eksik-secret dalı alarm veriyor, geçersiz-imza dalı vermiyor). `PADDLE_WEBHOOK_SECRET` yanlış
   girilirse her olay 401 olur, Paddle penceresi dolunca pes eder ve **iptal olmuş her org sonsuza
   kadar premium kalır**; ters yönde ödeyen müşterinin yükseltmesi `planCode`'a hiç ulaşmaz. Simetrik
   eksik: `status:"active"` ama `currentPeriodEnd` aylar önce geçmiş satırları kimse fark etmiyor
   (drift dedektörü yok; `paddleRequest` yalnız kullanıcı tetikli yollardan çağrılıyor).
   *Kapatılmadı: alarm eşiği + drift dedektörü tasarım kararı, para rotası.*
2. **Uygulama-içi yükseltmede `CheckoutConsent` yazılmıyor.** Yükseltme `prorated_immediately` —
   fark istek anında karttan çekiliyor — ama `legalVersion`/`legalTextHash`/`ip`/`userAgent` kaydı YOK;
   onay kutusu yalnız `!manageable` dalında çiziliyor, yani plan-değiştirme düğmeleri görünürken HİÇ
   çizilmiyor. Checkout akışı "kayıtsız ödeme olmaz" diye kurulmuşken ikinci gerçek tahsilat yolunda o
   kayıt yok. *Kapatılmadı: hukuki etkisi var (mesafeli satış), avukat paketiyle birlikte karar.*
3. **`paused` ve TANINMAYAN durum → `past_due` → 14 gün ücretsiz premium.** `paddle.ts:165-172`'nin
   yorumu *"Never gift premium access on an unrecognized value"* diyor ama `getEntitlement:126-132`
   `past_due`'ya dunning grace veriyor ve `pastDueSince` tam o anda damgalanıyor. Yorumun iddia ettiği
   değişmez, kodun uyguladığı değişmez değil. *Kapatılmadı: ödeyen müşterinin erişimini etkiler.*
4. **`trialing` + bayat `trialEndsAt`.** Webhook `trialEndsAt`i YALNIZ `status === "active"` iken
   temizliyor; Paddle tarafı `trialing` derse org'un lapse olmuş ters-deneme tarihi miras kalıyor ve
   **ödeyen müşteri "denemeniz doldu" ekranında kilitleniyor**. Ulaşılabilirlik canlı Paddle fiyatında
   deneme dönemi tanımlı mı sorusuna bağlı → **DOĞRULANMADI**. *Kapatılmadı: önce Paddle panelinden teyit.*
5. **`P2002` update dalında yakalanmıyor** (`route.ts:306-313`); create dalı yakalıyor. Ön koşulu
   zaten takipli "ikinci abonelik penceresi". Etki: org'un `status`/`planCode`'u donuyor, ama sesli
   (500 + `reportError`). *Kapatılmadı: düşük öncelik, ön koşulu ayrı madde.*

### B. Kimlik / oturum

6. **`exitImpersonation` `mfa` iddiasını DOĞRULANMAMIŞ çerezden yeniden üretiyor** (`admin.ts:98,127`
   `getSession()` — epoch kontrolü yok, DB okuması yok), kardeşi `enterOrganization` ise doğrulanmış
   oturumu parametre olarak alıyor. **Bugün sömürülebilir DEĞİL**: rota önce `requireSession` koşuyor
   ve `api.ts:93` düşürülmüş iddialı impersonation oturumunu zaten 401'liyor. *Kapatılmadı: fail-open
   ŞEKİL, canlı açık değil; imza değişikliği ayrı tur.*
7. **`resend-verification` tek zamanlama-paritesiz kimlik uç noktası.** Üç kardeşinin hepsinde bilinçli
   parite kodu var (`login` `dummyVerifyPassword`, `register` `hashPassword`, `forgot-password` bcrypt
   + `__timing_parity__` no-op yazma). Burada bilinen dal iki DB round-trip + şifreli payload üretiyor,
   bilinmeyen dal doğrudan 200 dönüyor. Outbox bayrağı AÇIKKEN farkın ölçülebilirliği **DOĞRULANMADI**
   (canlı gecikme ölçümü gerekir). *Kapatılmadı: 08-06'da ölçtüğüm gibi ölçülmeden düzeltilmez —
   "ölçtüm" demeden önce ölçümün hedefi vurduğu doğrulanır.*
8. **Uygulama-içi şifre yeniden-doğrulaması denetim izi bırakmıyor** (`2fa` setup + `account/delete`);
   `login` her yanlış şifrede `auth.login_failed` yazıyor ve `login-acct:{email}` kovasını tüketiyor.
   Çalınmış oturumu olan biri ~2k deneme/gün yapabiliyor ve ürün bunu GÖREMİYOR. *Kapatılmadı: hesap
   başına kova tasarımı, açık `forgot-req` kova kararıyla (↓ACIK-ISLER §3) BİRLİKTE yapılmalı.*

### C. Çok-kiracılı izolasyon
> Ajan 25 `[id]` rotasını, 81 API rotasını ve 22 sayfayı taradı: **sömürülebilir çapraz-kiracı IDOR YOK.**

9. **Middleware rolü JWT'den okuyor ve o claim'i her istekte 14 gün ileri taşıyor** (`middleware.ts:73,93`).
   ~17 panel sayfasının TEK rol kapısı bu; API tarafı DB-yetkili (`api.ts:53`). Ekip yönetimi gelirse
   düşürülmüş bir manager panel sayfalarını render etmeye devam eder. CLAUDE.md'nin *"ekip yönetimi
   eklenirse sessionEpoch bump ZORUNLU"* notu bir İNSAN kuralı, kodda zorlanmıyor. *Kapatılmadı: rol
   değiştiren yüzey henüz YOK; düzeltmesi layout rol kapısı ister ve `/tasks` muafiyeti olmadan sonsuz
   döngü olur (↓ACIK-ISLER §1 ile aynı tur).*
10. **`inbox/[id]` `include: { property: true, reservation: true }`** → `icalToken` + `chatToken`
    (ikisi de kimliksiz uç nokta bearer'ı) sunucuda okunuyor. **Bugün tarayıcıya GİTMİYOR** (sayfa
    yalnız skaler destructure ediyor). Bir refaktör nesneyi bir Client Component'e geçirirse takvim
    token'ı flight payload'ına biner. *Kapatılmadı: latent; `select` daraltması ayrı temizlik turu.*
11. **`stripPropertySecrets` sözleşmesi `reservations` rotalarını kapsamıyor** — `PATCH
    /api/reservations/[id]` ham satırı döndürüyor (`chatPinHash`, `chatBoundHash`). Kiracı-içi, düşük
    değer; asıl bulgu **sözleşme boşluğu**. *Kapatılmadı: ortak modüle taşıma + test genişletmesi.*

### D. AI hattı

12. **`access_security` hiçbir deterministik yoldan ÜRETİLEMİYOR.** `HIGH_STAKES_RISK_TYPES` içinde
    ama `detectRiskType` onu asla döndürmüyor → "kapı kodunu ver" sınıfında **tek dayanak modelin kendi
    beyanı**, yani kapının var olma sebebine aykırı. *Kapatılmadı ve BİLİNÇLİ: yeni bir kelime ağı
    ölçülmüş külliyat olmadan yazılmaz — 08-07'de tam bu şekilde 12 meşru KB kaleminin 6'sını elemiştim.
    Ayrı tur: külliyat + golden senaryolar (hem tehdit hem övgü-tuzağı) + iki yönlü mutasyon.*
13. **Prompt ÇİTİ yalnız `<<FENCE>>` biçimine karşı korunuyor.** Asıl yapı `═`×52 bar + başlık ve
    `[MİSAFİR]:`/`[OPERATİF]:` rol etiketleri; ikisi de ne `sanitizePromptValue`'da temizleniyor ne
    `INJECTION_PATTERNS`'te. Ajan 6/6 yapısal sahtelemenin geçtiğini ölçtü — **misafirin Airbnb görünen
    adı üzerinden de**. *Kapatılmadı: prompt hot-path'i; çit kaçışı (escaping) tasarımı ayrı tur.*
14. **`looksLikeSecret` geri çağırımı 7/25 kaçırıyor** (`kasa`, `kilit`, `alarm`, `zil`, `interkom`,
    çıplak `anahtar` çapaları yok; İngilizce "The code to get in is 4590" bitişiklik şartına takılmıyor).
    *Kapatılmadı: aynı 08-07 dersi — genişletme yanlış-pozitif ölçümüyle BİRLİKTE yapılır. Not: kategori
    bacağı (↑#6) bu sınıfın büyük kısmını zaten kapattı.*
15. **Dil paritesi: karbonmonoksit DE/FR/ES/RU/AR'da hiçbir deterministik ağa düşmüyor** (`kohlenmonoxid`,
    `monoxyde`, `monóxido`, `угарн…`, Arapça karşılığı listede yok) → org tier-2'yi açtıysa misafir bir
    CO alarmına "özür dileriz, ilgileneceğiz" kalıbı alabilir. Hamamböceği de DE/FR/ES'te eksik.
    *Kapatılmadı: saf EKLEME ve kısıtlayıcı yön, yani düşük riskli — ama dil listesi değişikliği golden
    set koşusu ister ve kullanıcı uyanıkken yapılması daha doğru (CLAUDE.md dil-paritesi kuralı).*
16. **Landing demosu `wouldAutoSend` döndürüyor** — kimliksiz, ücretsiz, üretim kapısının verdiğine dair
    ORACLE. Rota `intent`/`source`/`detectedLanguage`'ı tam da "sorgulanabilir harita" olmasın diye
    soymuş, sonra en değerli biti dönüyor. *Kapatılmadı: landing davranışını değiştirir (pazarlama
    yüzeyi), kullanıcı kararı.*

### E. KVKK / gizlilik

17. **Retention anonimleştirmesi rezervasyon başına TEK ATIMLIK.** Dal A `guestName: { not: ANON_NAME }`
    ile seçiyor → bir kez temizlenen konaklama SONSUZA DEK dışarıda kalıyor; dal B yalnız
    `reservationId: null` görüyor. Temizlenmiş bir konaklamaya SONRADAN yazılan misafir metni **iki
    süpürgenin de erişemediği** bir boşluğa düşüyor. `scrub-scope-parity` testi bunu göremez: o
    (model, kolon) KÜMELERİNİ karşılaştırıyor, sızan şey SEÇİCİ. **Bugün Hospitable bacağı için
    ulaşılamaz** (540 günlük pencere < 24 aylık cutoff) ama elle yanıt/QR/outbox için ZATEN canlı ve
    `DATA_RETENTION_MONTHS ≤ ~17` yapılırsa tamamen açılır — yani *gizlilik-dostu ayar* olan bozuyor.
    *Kapatılmadı: seçiciyi içerik-tabanlıya çevirmek KVKK süpürgesinin davranışını değiştirir; kullanıcı
    onayı + taze yedek ister.*
    🔬 **08-09 (2) — İKİ YÖN DE AMPİRİK OLARAK ÖLÇÜLDÜ (geçici probe, sonra silindi):**
    · **Sızıntı gerçek:** 40 ay önce çıkışlı bir rezervasyon süpürüldü (`guestName` → "Eski misafir",
      gövde → "[saklama süresi doldu — içerik silindi]"), sonra AYNI konuşmaya yeni bir inbound mesaj
      yazıldı ve süpürge TEKRAR koşturuldu → yeni gövde **birebir sağ kaldı** (`"Yeni mesaj: IBAN
      TR12 3456, tel 05329998877"`). Bu satır bir daha ASLA seçilmez: çapa `departureDate` ve o hiç
      ilerlemez, sentinel ise kalıcı. Ulaşılabilir senaryo sıradan: misafir yıllar sonra "şarj
      aletimi unuttum" / yorum-iadesi için yazar.
    · **AMA "içerik-tabanlı seçici" DÜZELTMESİNİN BEDELİ DE ÖLÇÜLDÜ:** öksüz dal o düzeltmeyi ZATEN
      almış durumda (`messages.some(inbound, body != ANON_BODY)`), ve yaş çapası eski kaldığında
      **BUGÜN gelen mesajı ilk koşuda sildi** (probe'un ikinci vakası tam bu yüzden "satır bulunamadı"
      diye patladı). Yani naif düzeltme, host'un cevaplamak için okuması gereken CANLI mesajı yok
      eder — `looksLikeSecret` turunun aynı sınıfı (koruma genişletirken ürünü kırmak).
    · Dolayısıyla iki ADAY MODEL var ve seçim ürün/hukuk kararıdır: **(A) rezervasyon-başına içerik
      koşulu** (çapa `departureDate`, taze mesaj anında silinir) · **(B) mesaj-başına çapa** — inbound
      mesaj KENDİ `createdAt`'i cutoff'tan eskiyse silinir, rezervasyondan bağımsız. (B) gizlilik
      metninin harfiyen söylediği şey ("24 aydan eski misafir PII'si anonimleşir"), taze veriyi yok
      etme riski YOK, süpürge zaten partili — **önerilen (B)**, ama KVKK yolunda davranış değişikliği
      olduğu için ONAY + taze yedek ister. UYGULANMADI.
18. **Gizlilik metni §15 kod-üstü bir GARANTİ veriyor** ("bu bilgiler otomatik yanıtlardan ayıklanır")
    oysa dedektörün ölçülmüş kaçırma oranı sıfır değil. *Kapatılmadı: hukuk metni — CLAUDE.md kuralı
    gereği avukat onayı olmadan DOKUNULMAZ. Öneri: §14'ün kendi dilindeki "teknik önlemler uygulanır"
    biçimine yumuşatma.*
19. **"Anonim hâle getirme" iddiası pseudonymisation'ı anlatıyor** — `sourceReference`,
    `externalReservationId`, `externalConversationId`, `Message.externalId` duruyor (dedupe/tombstone
    için ZORUNLU), yani satır kombinasyonla yeniden kimliklendirilebilir. `erasure.ts:17-22` bunu
    dürüstçe yazıyor; `data-retention.ts` ve gizlilik sayfası "anonim hâle getirilir" diyor.
    *Kapatılmadı: hukuk metni + politika belgesi, avukat paketi.*
20. **`Organization.aiStyleProfile` iki süpürgenin de DIŞINDA** ve `scrub-scope-parity`'nin model
    kümesinde YOK → misafir türevli içerik hem 24 aylık süpürgeden hem açık silme talebinden sağ çıkıyor;
    KVKK ihracatında da görünmüyor. ⚠️ ↑#5 ile örnekleme kaynağı düzeldi ama **profil metni hâlâ
    süpürgesiz**. *Kapatılmadı: süpürge kapsamı genişletmek migration'sız ama davranışsal; ayrıca
    `Organization`'ı kanarya kümesine eklemek ayrı karar.*
21. **Giden mesaj gövdesi ve ekip notlarında yalnız AD redakte ediliyor** (`redactNameFromBody`);
    host'un yazdığı telefon/e-posta aynı transaction'da `guestPhone`/`guestEmail` null'lanırken metinde
    KALIYOR. Gizlilik metni "iletişim bilgileri … silinir" diyor. *Kapatılmadı: `redactSensitive`
    kompozisyonu üç yüzeyde davranış değiştirir; ölçülmeden yapılmaz.*
22. **QR sohbeti 4. ingress yazarı ve tombstone kapısı YOK** — `erasure.ts` başlığı "her ingress
    yazarı" deyip üçünü sayıyor. Dar: `isOpenNow` penceresi + `GUEST_ERASURE_ENABLED` kapalı.
    *Kapatılmadı: ya kapıyı ekle ya başlığı düzelt — ikisi de bilinçli karar.*

### F. Altyapı

23. **~~`public/uploads` üretimde servis edilmiyor~~ → ✅ CANLI ARIZA DEĞİL (08-09, kullanıcı ekran görüntüsüyle kapattı).**
    Mekanizma DOĞRU ve pinlenmeye değer: gerçek `next start` ile ölçtüm — boot'tan ÖNCE var olan
    `public/uploads/x.txt` **200**, boot'tan SONRA yazılan (hem mevcut hem yeni klasöre) **404**.
    Next `public/`i boot'ta bir kez tarıyor, dosya-sistemi yeniden kontrolü `if (!matchedItem &&
    opts.dev)` ile dev'e kilitli. **AMA SONUCUM YANLIŞTI:** üretimde `STORAGE_ENABLED` AÇIK →
    fotoğraflar diske değil bucket'a gidiyor ve `/api/storage/photo/<key>` ROTASI üzerinden imzalı
    URL'e çözülüyor (kullanıcının paylaştığı canlı URL: `…t3.storageapi.dev/lixus-uploads-<sonek>/
    org/<org>/task/<task>/<dosya>.png?X-Amz-Algorithm=AWS4-HMAC-SHA256…`). Rota olduğu için boot
    anlık-görüntüsünden etkilenmiyor. **Doğru ifade: legacy DİSK yolu (`STORAGE_ENABLED` kapalıyken)
    çalışmaz — ve o yol bugün kullanılmıyor.**
    ⚠️ Yine de İKİ belge düzeltilmeli, çünkü ikisi de yanlış bir öncül taşıyor ve `STORAGE_ENABLED`
    bir gün kapatılırsa sessiz arızaya dönüşür: `DEPLOYMENT.md:229` ("eski `/uploads` doğrudan statik
    olarak sunulmaya devam eder") ve CLAUDE.md'nin "legacy `/uploads` kimlik doğrulamasız servis
    ediliyor" notu. `security/audit-baseline.json`'daki `sharp` triaj gerekçesi de aynı öncüle
    dayanıyor — ulaşılabilirlik iddiası zayıflar, muafiyetin kendisi geçerli kalır.
    ⚠️ **DERS:** "kod yolu böyle diyor" ile "üretimde böyle oluyor" ayrı iki iddia. Mekanizmayı
    ölçtüm ama hangi yolun CANLI olduğunu doğrulamadan sonuca atladım.

24. **`TRUSTED_PROXY_HOPS` boot doğrulaması YOK** (`env-check.mjs`'te hiç geçmiyor). `"2 hops"`,
    `"0x2"` gibi bir yazım sessizce 1'e düşer → 07-31'de kapatılan "tüm hız limitleri GLOBAL" arızası
    geri gelir ve KVKK rıza / ödeme onayı delil satırları yeniden edge adresini taşır.
    *Kapatılmadı: boot kapısı eklemek deploy bloklama riski taşır (aynı gerekçe `PADDLE_WEBHOOK_SECRET`
    maddesinde de var); tek karar olarak birlikte verilmeli.*
25. **`STORAGE_*` boot doğrulaması yalnız `STORAGE_ENABLED=1` iken koşuyor**, oysa iki çalışma-zamanı
    yolu `storageConfigured()`e (bayraktan BAĞIMSIZ) bakıyor → yarım yapılandırma sessizce aktifleşir,
    KVKK silme kuyruğu sonsuza kadar `pending` kalır ve alarm çıkmaz. *Kapatılmadı: bucket turuyla aynı.*
26. **Takvim beslemesinde PORT kısıtı yok** — `isPrivateHost` iç hedefleri doğru bloklıyor ama manager
    herhangi bir PUBLIC `host:port`a 10 besleme/daire yöneltebiliyor ve hata metinleri üç yönlü bir
    oracle (`HTTP <status>` / "ulaşılamadı" / "çok büyük") → Railway egress IP'siyle kör port tarayıcı.
    *Kapatılmadı: 443'e kısıtlamak gerçek PMS beslemelerini etkilemez ama besleme ekleme akışını
    daraltır; kullanıcı kararı.*
27. **`SENTRY_DSN` https pininin DIŞINDA** ve `parseDsn` şema kontrolü yapmıyor → `http://` bir DSN
    her hata zarfını (redakte edilmiş ama bilinen boşlukları olan) düz metin yollar. *Kapatılmadı:
    boot kapısı kararıyla birlikte (↑#24).*
28. **Üretim imajı 16 operatör script'inin tamamını taşıyor** (`COPY scripts ./scripts`), oysa yorumu
    yalnız iki env script'ini gerekçe gösteriyor — aralarında `restore-calendar-url-plaintext.ts`
    (Faz-4 sözleşmesini geri alır) var. Giriş noktası değil, **sömürü-sonrası kolaylık**.
    *Kapatılmadı: `COPY` daraltması Dockerfile değişikliği; imaj turu 08-05'te kapandı, tekrar açmak
    deploy riski.*

### G. Test bütünlüğü (boş pinler)

29. ✅ **KAPANDI (08-09 (2)).** ~~`api-route-scoping.test.ts` org-kapsam ölçütü `/organizationId/` düz metin
    taraması → org id'yi İSTEKTEN alan `admin/export` (`?orgId=`) ve `admin/impersonate` (gövde)
    "kapsanmış" sayılıyor.~~ **Bulgu ÖNCE ÖLÇÜLDÜ:** iki rotadan da `isSuperAdmin` satırı (+ artık
    kullanılmayan import'u) silinip TÜM süit koşuldu → **3254 test YEŞİL**. Yani kimliği doğrulanmış
    sıradan bir müşteri, başka org'un id'sini yazarak o org'un TÜM verisini indirebilir ve oturumunu
    o org'a devredebilirdi. **İki katmanlı kapatıldı:** (a) davranışsal pin
    `tests/integration/admin-superadmin-gate.test.ts` — `isSuperAdmin` BİLEREK mock'lanmaz, karar
    gerçek `SUPERADMIN_EMAILS` üzerinden koşar; 8 test: oturumsuz · sıradan müşteri · **superadmin
    ama `mfa:false`** · KONTROL (gerçek operatör başarılı). Zarar durum koduyla DEĞİL, sonuçla
    ölçülür: export yanıtında kurbanın adı GEÇMEZ + `data.export` audit satırı YAZILMAZ,
    impersonate'te `enterOrganization` HİÇ ÇAĞRILMAZ. (b) yapısal pin: `REQUEST_DERIVED_ORG_ID`
    (üç rota, `quality-audit` dahil) ölçütten düşürülüp gerekçeli listeye alındı. Beş mutasyon iki
    yönde kırmızı (kapıyı sil ×2 · koşulsuz 401 ×2 · ölçütü gevşet).
30. ✅ **KAPANDI (08-09 (2)).** ~~`NOT_ORG_SCOPED` gerekçeleri düz metin; `/api/cron/sync`,
    `/api/cron/email-outbox`, `/api/leads` üçünün de test dosyası YOK.~~ Üçü de artık DAVRANIŞSAL
    olarak sınanıyor (`tests/integration/cron-and-leads-auth.test.ts` + `leads-rate-limit.test.ts`).
    Kanıt durum koduyla DEĞİL YAN ETKİYLE ölçülüyor: yetkisiz istekte `runScheduledSync` /
    `drainEmailOutboxOnce` **hiç çağrılmıyor**, 429'lanan lead satırı yazılmıyor ve e-posta
    tetiklenmiyor. Kapsanan kenarlar: başlıksız · yanlış secret · **doğru UZUNLUKTA yanlış secret**
    (sabit-zamanlı dalın gerçekten koştuğu) · `?secret=` query reddi · `CRON_SECRET` boşken
    fail-closed · kontrol (iki başlık biçimi de geçer).
    🚨 **BİR MUTASYON DAVRANIŞSAL OLARAK YAKALANAMADI ve bu testin cinsinin sınırı:**
    `timingSafeEqual` → `provided === secret` yedi davranışsal testin HEPSİNİ yeşil geçiyor —
    sabit-zamanlı karşılaştırmanın tek gözlenebilir farkı SÜREDİR ve süre ölçen bir CI testi flaky
    olurdu. Depo bu kararı zaten vermişti (`forgot-password-timing-parity.test.ts`: "pinler SÜRE
    ÖLÇMEZ, YAPISALDIR") → aynı çözüm: `timingSafeEqual` + uzunluk ön-kontrolü VAR, `=== secret`
    anti-deseni YOK, popülasyon guard'lı. Uzunluk ön-kontrolünü silen mutasyon da kırmızı
    (o olmadan kapı 401 yerine 500 verir — `timingSafeEqual` eşit olmayan uzunlukta FIRLATIR).
    Toplam dokuz mutasyon iki yönde doğrulandı.
31. ✅ **KAPANDI (08-09 (2)).** ~~`admin` rotaları için `isSuperAdmin|requireSession|requireAuth`
    alternasyonu — `requireSession` tek başına tatmin ediyor.~~ Alternasyon KALKTI: ölçüt tek dizgi
    (`isSuperAdmin(`) ve artık **elle seçilmiş İKİ rotaya değil, `admin/` altındaki HEPSİNE** koşuyor
    (popülasyon guard'ı ≥7). TEK istisna `admin/exit` ve gerekçesi kapalı listede: yetkiyi ARTIRMAZ,
    DÜŞÜRÜR — istisnanın kendisi de pinli (`exitImpersonation` çağırır, `enterOrganization` /
    `buildOrganizationDataExport` ÇAĞIRMAZ), yani "exit" adı altında yetki artıran bir değişiklik
    kırmızı verir. Eski alternasyonu geri getiren mutasyon kırmızı doğrulandı.
32. **`login-account-lockout.test.ts:48` çapası korumasız** — `indexOf("const bodyResult")` -1
    dönerse `slice(0,-1)` neredeyse tüm dosyayı verir ve pin anlamsızlaşır (20 satır yukarıdaki
    kardeşi `expect(from).toBeGreaterThan(-1)` yazmış).
33. **`session.test.ts` HS256 pinini sınamıyor** — kardeşi `trusted-device.test.ts` tam bu sebeple
    HS512 sahte token üretiyor; daha yüksek değerli oturum token'ında o test yok.
34. **`audit-2026-08-07-round2.test.ts` ReDoS taraması yalnız `/…/i,` biçimindeki satırları görüyor**
    → farklı bayrak/yorum/çok satırlı yeni bir kalıp görünmez.
35. **`account-delete-billing-guard.test.ts` tarayıcısında POPÜLASYON GUARD'I yok** (kardeş taramaların
    hepsinde var) → eşleşme kaybolursa sıfır dosya incelenip test trivially geçer.
36. **`tests/helpers/tree-text.ts` yalnız 6 prop adını topluyor** → başka bir prop'tan render edilen
    PII `not.toContain` iddialarına görünmez.
37. **`paddle-webhook.test.ts` imza testi tek negatif vaka** — çapraz-gövde replay yok, başlıksız vaka
    yok; `timingSafeEqual` deponun BEŞ üretim dosyasında var ama **sıfır** test iddiası onu pinlemiyor.

> *Ajanın "sağlam" dediği ve DOKUNULMAMASI gerekenler:* `golden-scenarios` · `pinned-fetch` ·
> `rate-limit` · `api-secret-exposure` (ön koşul testiyle birlikte) · `session-payload-roundtrip` ·
> `require-auth-failclosed` · `reservations-import-cancelled` · `response-cache-policy` ·
> `audit-baseline` · `email-identity`.

---

## 📌 Bir CLAUDE.md düzeltmesi (ajan tespiti, kod-doğrulandı)
CLAUDE.md *"`--omit=dev` tek başına yetmez: `next`, `@playwright/test`'i OPSİYONEL PEER olarak bildiriyor
ve npm peer'ları otomatik kurar"* diyor. `next@15.5.23` onu `peerDependenciesMeta` altında
`{"optional": true}` işaretliyor ve **npm opsiyonel peer'ları otomatik KURMAZ** — yani runner imajı
notun iddia ettiğinden temiz. Not düzeltilecek.

## 📌 Doğrulanan iyi haberler
- Çapraz-kiracı IDOR **yok** (25 `[id]` rotası + 22 sayfa tarandı).
- SSRF sertleştirmesi **her dalda** geçerli; `fetchFeedText` tek host-girdili fetcher.
- Bağımlılıklar: 6 danışmanın **6'sı triaj edilmiş, 0 süresi geçmiş** → `audit-check.mjs` exit 0.
- CI izinleri dar, `pull_request_target` yok, fork PR'ları dışlanmış, üçüncü taraf action yok.
- Paddle imzası ham gövde üzerinden + `timingSafeEqual` + ±5 dk pencere; `previewToken`/`jti`
  tek-kullanım zinciri ve definitive/ambiguous ayrımı **doğru ve fail-closed**.
- Model çıktısı zorlaması sağlam: `NaN→0.5`, bilinmeyen `riskLevel` **fail-CLOSED** `high`'a,
  bilinmeyen intent `general`+conf≤0.5, `finish_reason=length` → fallback → kapı reddi.
