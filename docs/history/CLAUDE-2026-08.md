# Arşiv — kapanmış tur anlatıları (2026-08)

> Bu dosya CLAUDE.md'nin TARİHÇE bölümünden taşınan TAM anlatılardır. Oturum
> başında otomatik OKUNMAZ. Hiçbir satır silinmedi.
>
> ⚠️ Taşınmadan ÖNCE her anlatı canlı kural için tarandı; yalnız burada yaşayan
> kurallar CLAUDE.md'nin kalıcı bölümlerine TERFİ ETTİRİLDİ (görünmez karakter
> sınıfı · dil paritesi · kesme işareti · linkProperty org kapsamı · renewLock
> geçici-hata ayrımı · kalp atışı · geriye 540 gün · çit atomikliği ·
> delivery_unverified · impersonation 2FA kilidi · ENCRYPTION_KEY fallback +
> teşhis-önce-sil · operatör klonunda reset yasağı · operatör script tuzakları).
> Geri kalan (commit SHA'ları, test sayıları, süreç anlatısı, yedek dosya
> adları) yalnız burada.

- 08-02: **ŞİFRE SIFIRLAMA YAPISAL DÜZELTMESİ CANLIDA (m47) + Codex'in taşıyıcı itirazı.** Faz 0-1 additive
  migration + çift yazma, bayrak DEFAULT KAPALI (↑ayrı bölüm). **Codex itirazı haklı çıktı:** bağlantı
  `?t=` query'sindeydi ve bu, tasarım belgesinin KENDİ "log'da görünmez" gereksinimiyle çelişiyordu →
  fragment'e taşındı. Belgede yazılı olan `Referrer-Policy: no-referrer` de UYGULANMAMIŞTI (`next.config.mjs`'te
  yalnız `/c/:path*` vardı) — belge var olmayan bir korumayı anlatıyordu, artık gerçek ve test-pinli.
  Kanıtlanabilen taraf: repoda tarayıcı Sentry SDK'sı YOK, `captureToSentry` yalnız
  `context/errName/errMessage/detail` gönderiyor → istek URL'i hiç toplanmıyor.
  **Yedek (kullanıcı aldı, push ön şartı):** `lixus-prod-post-contract-2026-08-02-101312.dump`,
  1.427.338 bayt, TOC 190 girdi, SHA256 `A9F847A15C7B2005A555CBA45DA07FF6AEFDC84738FB41776D303563278A88BA`.
  **Operatör scriptleri artık PostgreSQL sürümünü OTOMATİK buluyor** (`Resolve-PgBin`, en yüksek kurulu
  sürüm): sabit `17` pini operatör makinesi 18'e geçince kırılmıştı. Yön kuralı: pg_dump/pg_restore
  sunucudan YENİ olabilir ama ESKİ OLAMAZ → en yüksek doğru seçim. Gerekli exe'lerin **HEPSİ** aynı
  klasörde aranır (client-only kurulumda `initdb`/`pg_ctl` yoktur; tek exe'ye bakan kontrol öyle bir
  kurulumu seçip provayı `initdb`'de patlatırdı). `-PgBin` elle vermek her zaman kazanır. ⚠️ Script'ler
  saf ASCII TUTULUR (PS 5.1 BOM'suz .ps1'i ANSI okur; kurtarma aracında bozuk yorum istenmez).
- 08-02 (2): **PROD'DA GERÇEK ALARM — `hospitable-token-undecryptable` (KAPANDI).** Log'da ikinci bir org
  için AES-GCM kimlik doğrulama hatası. **Teşhis aracı yazıldı:** `scripts/diagnose-hospitable-tokens.ts`
  (+ test edilebilir çekirdek `hospitable-token-diagnostics-core.ts`) — SALT-OKUMA, yalnız SAYI döndürür
  (token/ciphertext/hash/e-posta/org-id basacak alan YOK, sahte DB'de yazma metodu bile yok → "salt-okuma"
  yapısal olarak pinli). **Ayrım mantığı:** anahtar GENEL olarak yanlışsa aynı kutuyu kullanan HİÇBİR değer
  açılmaz → tek bir başarı bile "anahtar doğru" kanıtıdır. Bağımsız sonda olarak `User.twoFactorSecret` de
  sayılır. Opsiyonel `ALT_DECRYPT_KEY` sondası `crypto-core.key()`in `ENCRYPTION_KEY || AUTH_SECRET`
  sırasından doğan "anahtar set edilmeden ÖNCE yazılmış satır" hipotezini sınar.
  **SONUÇ (kullanıcı koştu):** sorun İZOLE'ydi — bozuk Hospitable test hesabı silindi, son teşhiste
  access/refresh token dolu satır **0**. Nuve zaten `HOSPITABLE_API_TOKEN` env fallback'iyle çalışıyor
  (şifreli kolonu yok) → alarm sustu. ⚠️ **KÖK NEDEN KESİN OLARAK BELİRLENEMEDİ** (eski anahtar mı, bozulma
  mı): satırlar silindiği için delil de gitti, `ALT_DECRYPT_KEY` sondası artık koşulamaz. Ödeyen ikinci bir
  müşteride TEKRARLARSA önce sondayı koş, SONRA silme. ⚠️ Hospitable bölümü artık 0 satır ölçüyor →
  anahtar kanıtı yalnız 2FA sondası + `verify-calendar-url-enc --post-contract` (8/8, 07-29) üzerinde.
> Bu bölüm bilinçli KISA. 52 turun tam anlatısı (49 KB) arşiv dosyasına taşındı —
> hiçbir satır silinmedi. Taşımadan önce her satır canlı kural için tarandı;
> yalnız orada yaşayan 5 kural ↑KALICI KARARLAR'a terfi ettirildi. Bir turun
> ayrıntısı gerekiyorsa arşive veya commit mesajına bak; buraya geri kopyalama.

- 08-01 (3): **SALDIRGAN DENETİM TURU — tek görünmez karakter DÖRT savunmayı birden kapatıyordu.** Kullanıcı "4 maddeyi ekle + 2 agentı daha daha derine gönder" dedi. Önce tasarlanmış dört madde uygulandı (hepsi migration'sız): `linkProperty` P2002 org-kapsamlı benimseme (⚠️ arama ORG KAPSAMLI olmak zorunda — kısıt GLOBAL, yabancı org'un satırını benimsemek çapraz-kiracı sızıntısı; testle pinli) · `reservationsUnwritable` sayacı (dönüş tipini değiştirmek yerine sayım ÇAĞIRANDA = düşük risk) · ilerleme-tetikli kilit kalp atışı (⚠️ `setInterval` DEĞİL — asılı koşu kilidi sonsuza kadar tutmasın) · `CALL_BUDGET_MS` (Retry-After tavanı DENEME başınaydı: tek çağrı 6 dk uyuyabiliyor, üç ardışık 429 kilit TTL'ini aşıyordu). ⚠️ Kalp atışı testimin İLK hâli mutasyonu YAKALAMADI (kilidi koşu SIRASINDA gözlemlemiyordu) → ayrı dosyaya `syncHospitable` mock'uyla gerçekten ölçen test. **SONRA İKİ DERİN AJAN — ~1.577 girdi ölçüldü, YEDİ gerçek bulgu:** 🚨 **`normalizeForMatch` beş kod noktası siliyordu, 1.152'si vetoyu deliyordu.** Ürünün TÜM deterministik savunması bu tek fonksiyonun ürettiği metin üzerine kurulu. **U+00AD (SOFT HYPHEN — çoğu klavyede tek tuş, hiçbir yerde görünmez)** tek başına DÖRT savunmayı kapatıyordu: injection vetosu · misafir ADINDAKİ injection (Airbnb kontrollü metin) · holding-ack kapısı · `statedCheckoutTime` injection vetosu. `passesAutoReplySafetyGate` **TRUE** dönüyordu, yani misafire OTO-GÖNDERİM izni. **KURAL: görünmez karakter sınıfı `\p{Cf}` OLMAK ZORUNDA (+U+034F +varyasyon seçicileri); tek tek kod noktası listelemek YETMEZ. `\p{Mn}`'in tamamı EKLENMEZ — Türkçe/Arapça ayırıcı işaretler anlam taşır.** · Emoji beyaz listesi yalnız HARFSİZ dala uygulanıyordu: `isClosingAck("🆘")=false` ama `isClosingAck("tamam 🆘")=TRUE` (aynı dosyada ters yön) — bir kelime eklemek korumayı devre dışı bırakıyordu · Cümle sınırı garantisi TEK bağlaçla deliniyordu ("18:00. Yarın çıkıyoruz" RED ama "18:00 ve yarın çıkıyoruz" KABUL; 21 bağlaçtan 15'i deliyordu); ayrıca ipucu düz altdizi taramasıydı ("Çikolata … 15:00'te" → KABUL) ve ulaç nötrleme yalnız `-madan`ı kapsıyordu ("Çıkarken 09:00'da" → KABUL) · `SAFETY_CRITICAL_WORDS` yalnız TR+EN'di — `KEYWORDS.complaint` beş dil taşıyordu, bu liste taşımıyordu: DE/FR/ES/RU/AR yangın ve tıbbi acilde deterministik ağ **SIFIR**di ve kapı geçiyordu · **CLAIM-THEN-NOTIFY'ı kendi kodumda ihlal ettim**: yaşam-döngüsü alarmı pencereyi atomik claim edip `void reportError` çağırıyordu → tek Resend 5xx'i pencereyi tüketir, host 6 saat sessiz kalırdı · KVKK: yetim konuşmadan doğan görev İKİ süpürgeden de kaçıyordu (yetim dalı `Task`'a hiç dokunmuyordu) → artık `sourceMessageId` ile bağlı. **Ayrıca ARIZA-MODU ajanı üç şey daha buldu, UYGULANMADI (↓AÇIK İŞLER):** outbox açıkken `human_request` mesajı KENDİNİ veto ediyor (bayrak açılmadan önce kapatılmalı) · çit geri alınmıyor (rezervasyon sonradan yazılabilir hâle gelirse host aktif rezervasyonda "Konaklama bitti" görüyor) · `renewLock` `count`'u okumuyor (kilidi kaybetsek de döngü devam ediyor — "sonucu okunmayan await" kokusu). **2424 test.**
- 08-01 (4): **BEŞ AJAN TURU — 4 REGRESYONUM + OAuth'ta P1 bağlantı kaybı.** Kullanıcı "durum nedir, yorgunluk var mı, bunlar gerçekten işe yarıyor mu, agentlar bitince 2 tane daha koşsun" dedi. **KENDİ AÇTIĞIM DÖRT REGRESYON (hepsini AJANLAR buldu, ben değil):** (1) `applyFailureEffect`'te `autoReplyHoldUntil` yazmak → 402 sonrası abonelik yenilenince mesaj `canceled` olup KALICI kayboluyordu (↑hold=kilit kuralı; **sabah düzelttiğim "enqueue'de hold" arızasının AYNI SINIFI, aynı gün ikinci kez**); (2) alarm penceresini `epoch 0`'a çekmek → `reportError`'ün 1 dk geri çekilmesiyle birleşince 2 dakikalık cron'da sönümleme SIFIR (2+9=11 dk > 10 dk throttle → 90 deneme/saat; pencerenin varlık sebebi tam da o seldi) → 15 dk geri çekilme, ölçülen sonuç 12/saat; (3) `renewLock`'ta DB hıçkırığını "kilit kaybı" saymak → geçici bir hatada HİÇBİR org işlenmeden her geçiş bitiyordu (kilit kaybı TTL'in GERÇEKTEN dolmasını gerektirir; "güvenli yön durmaktır" gerekçem YANLIŞTI); (4) çit geri almasını iki ayrı yazmaya bölmek → birincisi tek-atımlık bağ geçişini tüketiyor, ikincisi düşerse konuşma kalıcı çitli kalıyordu (tek `$transaction`). **P1 (ajan 2, ilk ajanın KAÇIRDIĞI):** OAuth token isteğinde HTTP 200 alınmış ama gövdesi okunamamış yanıt `authFailure:TRUE` sınıflanıyordu → çağıran kiracının Hospitable bağlantısını SİLİYOR; tek bir ağ titremesi ödeyen müşterinin tüm misafir mesajlaşmasını durduruyordu. Aynı yerde refresh-token ROTASYONU: persist düşerse harcanmış token DB'de kalıp bağlantıyı bir sonraki turda öldürüyordu → persist kendi try'ında + tek yeniden deneme + ayrı alarm. **Diğer gerçek bulgular:** `review` (BELİRSİZ, mesaj iletilmiş OLABİLİR) konuşmaya kesin "iletilemedi" yazıyordu ve etiket host'u elle yanıtlamaya çağırdığı için misafire ÇİFT mesaj riski açıyordu → ayrı `delivery_unverified` kodu+metni · host'un elle gönderdiği mesajın hatası konuşmaya AI sebebi yazıyordu · şikayet eskalasyonu / deneme hatırlatmaları / bekletme mesajı / outbox yaşam-döngüsü enqueue'ları hepsi sessiz `catch {}` içindeydi · depolama silme kuyruğunda backoff yazması düşerse backoff HİÇ büyümüyordu (saatte 750 sağlayıcı DELETE; bucket açılmadan kapatıldı) · teslim edilmemiş taslak model geçmişine giriyordu · `/sent` teslim edilmemişleri "gönderildi" sayıyordu (`reports.ts` ZATEN ayıklıyordu). **DERS: bir günde dört kendi regresyonum + iki vacuous test.** Kalite düşüşü değil, KAPSAM etkisi: birbirine bağlı bir sistemde her düzeltme yeni bir yüzey açıyor ve mutasyon testi yalnız BENİM düşündüğüm senaryoyu sınar. Dördünü de bağımsız ajanlar yakaladı → **her düzeltme turundan sonra ajan turu ŞART, opsiyonel değil.** **2456 test.**
- 08-01 (3): **KUYRUK YOLUNUN SESSİZ KALICI ARIZALARI — dört ajan turu, 13 bulgu uygulandı, ikisi KENDİ yazdığım kod.** Kullanıcı "4 maddeyi ekle · 2 agent gönder · sonra daha da derine inen 2 agent daha · commit doğru mu emin ol" dedi. **KENDİ AÇTIĞIM İKİ ARIZA (ajanlar buldu):** (1) `human_request` devir hold'unu kuyruk yolunda ENQUEUE anında yazıyordum — hold aynı zamanda worker'ın `aiSendVeto` kapısıdır ("ai_paused") → worker AYNI geçişte KENDİ satırımızı veto edip `canceled` yapıyordu: tasarlanmış devir mesajı misafire HİÇ gitmiyor, üstüne konuşma 12 saat susuyordu. Hold artık teslimat ONAYLANINCA (`applyDeliveryEffect`→`applyHandoffHold`) kuruluyor — satır içi yolun semantiğiyle birebir ("önce mesaj gider, SONRA AI susar"); teslim başarısızsa hold HİÇ kurulmaz. Eski test bugu PİNLİYORDU, yeniden yazıldı. (2) `reportLifecycleSendFailures`'a yazdığım "pencereyi geri al" bloğu **ÖLÜ KODDU**: `reportError` ASLA FIRLATMAZ, `catch` hiç çalışmıyordu ve yorum var olmayan bir korumayı anlatıyordu. **UYGULANANLAR:** `reportError` artık `ReportOutcome {notified, throttled, configured}` döndürüyor ve e-posta bacağı `send` yerine `sendReporting` kullanıyor (raportörün kendisi CLAUDE.md'nin "bildirim yollarında `send` KULLANILMAZ" kuralını çiğniyordu) · kuyruk yolu KALICI hatada (`failed`/`blocked`/`review`) konuşmaya HİÇBİR ŞEY yazmıyordu → `applyFailureEffect` (sebep kodu + `autoReplyHoldUntil`, yalnız `status:"new"` satıra) · `already_queued` damgalanmıyordu → sonsuz modelleme (dedupe satırın DURUMUNA bakmaz, terminal satırın anahtarı SONSUZA KADAR tutulur) · devir penceresi teslimat-etkisi ONARICISINA bağlanmamıştı (İKİ ajan bağımsız buldu; pencere `sentAt`'ten hesaplanır, `now`'dan DEĞİL) · `/sent` ekranı teslim edilmemiş taslakları "gönderildi" sayıyordu (`reports.ts` ZATEN ayıklıyordu → iki ekran aynı soruya farklı cevap veriyordu) · teslim edilmemiş taslak MODEL GEÇMİŞİNE giriyordu (filtre yalnız son mesaj outbound iken koşuyordu; misafir sonra yazdıysa atlanıyordu) · senkron kilidi `renewLock`'un `count`'u HİÇ okunmuyordu → kilit sessizce kaybediliyor, koşu devam ediyordu (artık `break` + alarm) · `emailService.send` kaynaktan TAMAMEN çıktı (3 çağrı; `/api/leads`'teki "hata görünür olsun" try/catch'i sağlayıcı 5xx'inde HİÇ çalışmıyordu) · yaşam-döngüsü damga geri almaları ve satır içi claim geri alması sessiz `catch {}` içindeydi · çit geri almasında oku-yaz yarışı (sebep koşulu WHERE'e taşındı) · Paddle `unmapped-price` context'i SABİTTİ ama pencere (org, fiyat) başına → ikinci org 10 dk içinde gelirse 30 GÜNLÜK pencere hiç e-posta gitmeden yanıyordu. **🚨 DERS — İKİ TESTİM MUTASYONDA YEŞİL KALDI:** (a) "problem thread'inde sebep yazılmaz" tuzağı VACUOUS'tu — `aiSendVeto` problem/closed satırı DAHA ÖNCE iptal ediyor, yani o senaryo kapıya hiç ulaşmıyordu ("waiting" ile değiştirildi); (b) `expect(JSON.stringify(calls)).not.toContain(body)` — gövde satır sonu taşıyor, JSON'da `\n` olarak kaçışlanıyor, ham string JSON içinde BİREBİR HİÇ geçmez → assertion daima geçerdi. **KURAL: assertion AYRIŞTIRILMIŞ payload üzerinden yazılır, JSON string'i üzerinden ASLA; ve her tuzak testi mutasyonla İKİ YÖNDE sınanır (koruma kaldırılınca VE koşulsuz yapılınca).** **2448 test.**
- 08-01 (2): **B+C+D KOVALARI + DÖRT DENETİM AJANI — 19 bulgu uygulandı, ajanlar ÜÇ regresyonumu yakaladı.** Kullanıcı "B, C, D'yi yap, her turu bitirince agentlara baktır" dedi. Yöntem: her bulgu ÖNCE ajana doğrulatıldı, sonra kendim kodla/ölçerek teyit ettim, uyguladım, mutasyonla sınadım; tur bitince DÖRT ayrı denetim ajanı (regresyon · güvenlik · müşteri gözü · doğruluk) koştu. **AJANLAR ÜÇ BULGUNUN ÖNERDİĞİ DÜZELTMEYİ ÇÜRÜTTÜ:** (a) "rezervasyonsuz yazana cevap verme" ürün davranışını kırardı (preBookingBlock = satış yolu, test-pinli) → yerine sağlayıcı DURUMU tabanlı dar çit; (b) `{ not: X }` SQL'de NULL satırları EŞLEŞTİRMEZ, bulgunun fix'i kendi senaryosunu düzeltmiyordu; (c) ikinci-abonelik kapısı asıl pencereyi (ödeme↔webhook arası) kapatmıyordu. Bir ajan da bulgunun KAÇIRDIĞI asıl yolu buldu: `withSyncLock` in-process `running` bayrağını okumuyordu → tek replikada bile ulaşılabilir tek eşzamanlılık yolu manuel "Mesajları çek" butonuydu. **UYGULANANLAR:** denetim kaydı impersonation'da MÜŞTERİYİ fail gösteriyordu (8 çağrı yeri; para hareketi + KVKK silme kaydı dahil) → `auditActor` tek kaynak + kaynak pini · operatör müşterinin hesabında **2FA kurabiliyordu** (müşteri kendi hesabına bir daha giremezdi) → `account/delete` emsali kapı · ikinci Paddle aboneliği sunucu kapısı · bilinmeyen Paddle fiyatında sessiz plan bozulması → süre-tabanlı alarm · risk etiketinin bayat kalması · `statedCheckoutTime` virgül tuzağı + olumsuzlama vetosu + injection/rule_violation'da yazmama · devir rozetinin kesilmiş pencereden hesaplanması · imdat emojisinin "kapanış onayı" sayılması (ölçüm: tek-emoji girdilerin ~%98'i) → BEYAZ LİSTE · bağsız+iptal konuşmada oto-yanıt kapısının hiç çalışmaması · dört ÖLÜ test (biri SIFIR assertion çalıştırıyordu, biri sıralamayı hiç ölçmüyordu, biri kuralın anlatıldığı yolu hiç test etmiyordu). **🚨 EN ÖNEMLİ DERS — KENDİ AÇTIĞIM ÜÇ REGRESYON:** (1) bir mutasyon GERİ ALMA script'inde **sayı sınırı olmayan `replace`** kullandım; ileri yönde `assert count==1` vardı ama geri alma TÜM eşleşmeleri çevirdi → `sendDueAlerts` ve `previewChannelAutoReplies` sıralaması sessizce `desc`→`asc` oldu. Şikayet uyarısı, 50 bayat `status:"new"` satır biriktiğinde KALICI susardı (yaş filtresi SQL'de değil JS'teydi). Commit mesajında yoktu, yorum kodun tersini anlatıyordu, 2373 yeşil testin hiçbiri sıralamayı ölçmüyordu. **KURAL: mutasyon geri alması dosya yedeğiyle yapılır (`cp .bak`), `replace` ile ASLA.** (2) virgül ayırıcısı eklerken koyduğum ileri-bakış TÜM ayırıcılar için koşuyordu → cümle sınırını aşıp halüsinasyon sınıfını ters yönden geri açtı ("We leave tomorrow. Dinner at 18:00." → kabul). İKİ SEVİYELİ bölmeye çevrildi. (3) `PROBLEM_NEGATIONS`'tan çapasız önekleri kaldırırken yerine koyduğum tam biçimler eksikti → MEMNUN misafirin vedası şikayet sayılıyordu ("Hiçbir sorun çıkmadı, ev çok temizdi"). **ÜÇÜ DE MUTASYON TESTİNDEN GEÇMİŞTİ** — çünkü mutasyon yalnız YAZDIĞIM testin kapsadığını sınar; kapsam dışı davranışı yalnız bağımsız bir okuyucu görür. Ajan turu bu yüzden şart. Ayrıca KVKK: `auditImpersonation` operatörün e-postasını yazıyordu ve `AuditLog.metadataJson` müşterinin KENDİ veri ihracına ham giriyor → üçüncü kişinin kişisel verisi müşteriye aktarılıyordu, alan kaldırıldı. **2380 test.**
- 08-01: **DERİN DENETİM TURU — 52 bulgu raporlandı (`docs/DENETIM-BULGULARI-2026-08-01.md`), 11'i uygulandı, hepsi mutasyon-doğrulamalı.** Tur boyunca kural: bulgu KODLA doğrulanmadan uygulanmadı; ikisi (injection + negasyon) çalıştırılarak AMPİRİK ölçüldü. **(1) KALICI 4xx SONSUZ DÖNGÜ:** satır içi gönderim (üretim varsayılanı) kalıcı hatada claim'i geri alıyor ama `runDueChannelAutoReplies` `send_failed`'i "geçici" sayıp damgalamıyordu → aynı konuşma 2 dakikada bir yeniden modelleniyor, her turda bir model çağrısı + bir kota birimi yanıyordu. TEK bozuk konuşma org'un 150'lik günlük kotasını ~5 saatte tüketip gerçek misafirleri `daily_budget`'a düşürebiliyordu (Nuve'nin Hospitable'ı bugün 402 = tam bu senaryo). Çözüm **DAMGA DEĞİL GERİ ÇEKİLME**: `autoReplyHoldUntil` (429→15 dk, diğer 4xx→4 saat), sınıflandırma outbox'la aynı kaynaktan (`classifySendResult`) → iki yol ayrışamaz. Sebep artık KOD olarak yazılıyor (`send_failed`/`subscription_inactive`/`rate_limited`; ham sağlayıcı metni ASLA DB'ye). Belirsiz (5xx/timeout) davranışı AYNEN korundu. **(2) KVKK — görev açıklaması:** şikayet/akıllı görev misafirin HAM mesajını `Task.description`'a yazıyordu ve hiçbir süpürgede yoktu; DAHA KÖTÜSÜ şikayet görevi `reservationId`'siz yaratılıyordu, iki süpürge de kapsamı rezervasyondan kurduğu için satır İKİSİ İÇİN DE görünmezdi → başlıktaki misafir ADI hem süre-bazlı anonimleştirmeden hem AÇIK SİLME'den sağ çıkıyordu. **(3) SİLİNEN MİSAFİRİN OUTBOX SATIRI DİRİLİYORDU:** süpürgeler yalnız `pending`+`ambiguous` iptal ediyordu; `blocked` (402) `reactivateBlockedOutbox` ile OTOMATİK, `failed`/`review` ops ekranından insan eliyle pending'e dönebiliyor → silinmiş misafire aylar sonra sentinel mesaj. Kapsam tek kaynağa bağlandı (`ERASABLE_STATUSES`, outbox/state.ts) + ikinci savunma: gövdesi `ANON_BODY` olan satır ne reaktive edilir ne requeue. İstisnalar gerekçeli ve testli (`sent` teslim edildi · `canceled` ölü · `sending`/`reconciling` claim'li). **(4) `outside_hours` ÜRETİMDE ÖLÜ KODDU:** 07-31'de eklenen görünürlük düzeltmesi `applyChannelAutoReply` içindeydi ama tek üretim çağıranı aynı kontrolü org seviyesinde yapıp hiçbir konuşmaya dokunmadan dönüyordu; testler yeşildi çünkü fonksiyonu DOĞRUDAN çağırıyorlardı. Şema varsayılanı hâlâ gece-only olduğu için MEVCUT tüm org'ları ilgilendiriyordu. Artık erken dönüşten önce tek `updateMany` (model çağrısı yok, `skippedReason: null` koşulu gerçek sebebi ezmez + konuşma başına tek yazma). Aday kümesi tek kaynak: `dueAutoReplyWhere`. **(5) YAŞAM-DÖNGÜSÜ HATALARI SESSİZDİ:** üç göndericinin (karşılama/giriş/çıkış) hata dalı hiçbir yere yazmıyordu — giriş talimatı KAPI KODU taşıdığı hâlde. Koşu başına tek toplu, PII'siz alarm (sınıf + sayı). **(6) ŞİKAYET UYARISI SENKRONA BAĞLIYDI:** `sendDueAlerts` "bütçeden muaf"tı ama çağrısı `syncHospitable`'ın try'ının İÇİNDEydi → Hospitable fırlattığı anda (402 = Nuve'nin bugünkü hâli) uyarı geçişi HİÇ koşmuyordu; muafiyet bütçeye karşı vardı, İSTİSNAYA karşı yoktu. Org döngüsü üçe ayrıldı; **sıra korundu** (uyarı otomasyondan ÖNCE = oto-yanıtın ikinci savunması). **(7) GÜVENLİK KAPISINDA İKİ SESSİZ DELİK (ampirik):** injection vetosu ÇİFT BOŞLUK / satır sonu / U+00A0 ile deliniyordu (kalıplar tek ASCII boşlukla yazılı, metin hiç normalize edilmiyordu) → `normalizeForMatch` (sıfır-genişlik siler + boşluk tekler) YALNIZ kısıtlayıcı yollarda; beyaz listelere ve negasyon kontrolüne UYGULANMADI (KATLAMA KURALI ile aynı gerekçe). Ve `PROBLEM_NEGATIONS` çapasız önek içeriyordu (`"sorun yaşama"` → "sorun yaşamaktayız"i siliyordu) → Türkçenin en yaygın kibar şikayet kalıbı DETERMİNİSTİK olarak şikayet sayılmıyordu. **KURAL: negasyon listesi ÖNEK İÇEREMEZ, yalnız fiili olumsuz TAM biçimler.** **(8) QR:** `suggestReply` çağıran 8 yerden 7'si org bütçesini tüketiyordu, QR TEK istisnaydı — ve kimlik doğrulaması olmayan tek AI yüzeyi (25 daire × 200 = 5.000 çağrı vs org tavanı 1.500); bağlandı. Eskalasyon metni KOŞULSUZ "ev sahibine ilettim" diyordu ama tek push kanalı varsayılan KAPALI ve QR thread'i hiçbir dikkat yüzeyine düşmüyor → metin artık bayrağa göre dürüst (bayrak AÇILMADI, panel bandı ÖNERİLMEDİ — o UI kararı 07-16'da geri alınmıştı). **MIGRATION GEREKENLER UYGULANMADI**, `docs/MIGRATION-BEKLEYEN-ISLER.md` §4'e yazıldı: yaşam-döngüsünde belirsiz hatada damga tutulduğu için önizlemenin "gönderildi" iddiası KANITSIZ (üçüncü durum = kolon) ve kalıcı 4xx'te sonsuz tekrar (`Reservation`'da `autoReplyHoldUntil` eşdeğeri yok). **2304 test.**
- 07-31 (2): **SESSİZ KAYIP TURU — üç arıza de aynı desendendi: "yan etki başarısız oldu ama satır zaten işaretlenmişti".** (1) **Şikayet eskalasyon e-postası KALICI kaybolabiliyordu** (`c515bca`): `sendDueAlerts` konuşmayı atomik olarak new→problem yapıp SONRA `emailService.send()` çağırıyordu — `send()` ASLA fırlatmaz (sağlayıcı hatasında yalnız console'a yazar) ve dönüşü okunmuyordu. Resend'in tek bir 5xx'i host'a giden acil bildirimi yutuyor, satır bir daha SEÇİLEMİYORDU (bu döngü yalnız status "new" seçer; model yolu "problem" görünce erken döner) → retry YOKTU. Aynı arıza model yolunda da vardı (`void emailService.send`) ve oradaki catch yorumu "next sync cycle retries via sendDueAlerts" diyordu — **bu iddia yanlıştı**. Çözüm: `sendReporting` + başarısızlıkta claim geri alma (**YALNIZ `status`; `skippedReason`/`lastRiskType` KALIR** → inbox rozeti kaybolmaz, satır yeniden seçilebilir) + koşu başına tek toplu `reportError`. Ack/görev o turda atlanır (idempotency kilidi claim olduğu için yine en fazla bir kez gider). Sonsuz döngü riski yok: `ALERT_MAX_AGE_MS` dışına çıkan mesaj aday listesinden düşer (testli). (2) **Oto-yanıt gönderim hataları hiçbir yere yazılmıyordu** — `applyChannelAutoReply` hatayı string döndürüyor, `runDueChannelAutoReplies` onu okuyan TEK yer ama yalnız `sent` sayıyordu; artık sebep ETİKETLERİ (id/isim/gövde ASLA) koşu başına tek aggregate alarma gidiyor. (3) **Sync süre bütçesi şikayet uyarılarını da atlıyordu** → `sendDueAlerts` artık bütçeden MUAF (dış API yok, ≤50 konuşma); `totals` sayaçları import'un hemen ardına alındı (satırlar DB'ye yazılmışken koşu raporu 0 diyordu). Bütçe yorumu koda eşitlendi: **tavanlar ÇALIŞAN işi KESMEZ, yalnız yeni iş başlatmaz** (JS tek iş parçacıklı) — TTL aşımı imkânsız DEĞİL, sadece uzak; gerçek garanti heartbeat'li kilit ister (ayrı iş). **PLAN SINIRLARI DEKORATİFTİ:** 15/30/60 + 3.000 karakter yalnız POST'ta uygulanıyordu; PATCH'te *pasifle→yeni ekle→eskiyi geri aç* döngüsü adet sınırını SINIRSIZ deliyor, PATCH karakter tavanı hiç yokken zod 20.000'e izin veriyor (tavanın VARLIK SEBEBİ olan "her şeyi tek kayda doldur" kaçışı geri açık), COPY rotası hedef daireye saymadan satır yaratıyordu. Üçü kapatıldı, **mutasyonla doğrulandı**. ⚠️ **Eski uzun kayıtlar KİLİTLENMEZ: yalnız BÜYÜME reddedilir, kısaltma her zaman serbest** (yoksa kendi kaydını kaydedemeyen müşteri yaratırdık) — zod'un 20.000'i bu yüzden bilerek indirilmedi. `limitsForOrg` KULLANIM kapılarında fail-OPEN yapıldı (DB hıçkırığı ödeyen müşteriyi 500'lüyordu). `KB_ITEM_CAP` gerçekten tek kaynak (üretim 40 okuyordu, QR'da hiç tavan yoktu → ekrandaki "AI en fazla 30 okur" uyarısı yalandı). **AI YANITINDA İKİ AYRI "2000" VARDI, YALNIZ BİRİ GÜVENLİYDİ:** `max_completion_tokens=2000` aşılırsa `finish_reason=length` → fallback → kapı reddeder (güvenliydi); ama `reply.slice(0,2000)` TAM ve geçerli bir JSON'u cümlenin ortasından SESSİZCE kesiyor ve `source` hâlâ "openai" kaldığı için kapı bu YARIM mesajı misafire otomatik gönderebiliyordu. Tavan 4.000 (pratikte erişilemez; 2.000 tamamlama token'ı Türkçede ~2.500-3.000 karakter) + tavana çarpmak artık OLAY: rapor + güven 0.5'e clamp → yalnız taslak. **ÇOK-SORULUDA PROMPT KENDİYLE ÇELİŞİYORDU:** "TÜM soruları yanıtla" (Niyet) vs "tek konu, tek mesaj" (Spam) ve öncelik listesi Spam'i ÜSTE koyuyordu → modelin elinde soruyu ATLAMAYI söyleyen ÖNCELİKLİ kural vardı. Üstüne blok blok yazılan 6 soru ~20-30 kelime tuttuğu için uzunluk ipucu "2-4 cümle" diyordu. `countGuestAsks` (soru işareti VE satır sayısının BÜYÜĞÜ — Türkçede sorular çoğu kez işaretsiz ve alt alta gelir: "saat kaçta çıkış var\nnasılsınız\nçöp nerde" = 3 istek, 0 soru işareti) ile 2+ istekte uzunluk kuralı açıkça EZİLİYOR, iki çelişen kurala istisna yazıldı, ÖRNEK 24 eklendi. **Bağlantı kontrolü model çağrısının ÖNÜNE alındı:** `not_connected` bilerek damgalanmıyordu + kontrol modelin ALTINDAYDI → Hospitable'ı kopmuş org'un her cevapsız konuşması HER 2 DAKİKADA yeniden modelleniyor, taslak hiçbir yere yazılmadan atılıyordu (sonsuza kadar). **Sync pencereleri (hepsi env-ezilebilir):** sık geçiş geriye 90→30, geniş geçiş ileri 540→365. ⚠️ **Geniş geçişin GERİYE 540'ı bilerek AYNI KALDI** — mesaj importu rezervasyon penceresinden yürüdüğü için daraltmak, eski konaklamadan yazan misafiri sessizce kaybetmek olurdu. Müşteri gözü: "Mesajlar çekiliyor… bu birkaç dakika sürebilir", KB hata mesajları artık `fields`'tan okunuyor (özenle yazılan plan-sınırı metinleri "Doğrulama hatası" olarak görünüyordu). **2172 test.**
- 07-30 (2): **YEDEKTEN GERİ YÜKLEME PROVASI BAŞARILI (kullanıcı koştu, Codex onaylı) — kurtarma yolu artık KANITLI.** Önce taze POST-CONTRACT yedek alındı: `lixus-prod-post-contract-2026-07-30-110853.dump`, 1.426.608 bayt, TOC 190 girdi (yorum satırları hariç sayım; eski "205" ham satırdı — fark metodolojik, kayıp değil), SHA256 `9D9E3EFA4C32509E5A88283521CE29EA238B0B95246F8FFBF16DFDD0FC0FF849`, `pg_restore -l` doğrulamalı; eski pre-phase4 dump'a dokunulmadı (arşiv). Prova: GUID'li geçici PG17 kümesi (127.0.0.1:5599, prod'a sıfır temas) → **restore 0,7 sn exit=0** → fail-closed ASSERT'ler geçti (migration=47, bitmemiş=0, CalendarSource=8, sentinel=8) → `verify --post-contract` 8/8 sağlıklı, tüm hata sayaçları 0 → küme+GUID klasörleri+gizli env'ler silindi. **"Yedek + ENCRYPTION_KEY = çalışan kurtarma" iddiası artık ölçülmüş gerçek** (çekirdek restore <1 sn; darboğaz kurulum/anahtar-erişimi). ⚠️ **Gelecek provalar için Windows dersleri (üçü de canlıda yaşandı):** (1) `pg_ctl start`'a PowerShell çıktı yönlendirmesi (`*> $null`) BAĞLANMAZ — daemon konsol tanıtıcılarını miras alır, pencere sonsuz asılı kalır; `-s -w` kullan. (2) Türkçe Windows'ta `initdb` `--locale=C` İSTER ("Turkish_Türkiye.1254 non-ASCII" hatası). (3) PS 5.1 native argümanlardaki gömülü çift tırnağı BOZAR (`"Organization"` → `organization`) — büyük/küçük harfli tablo adı içeren SQL asla `-c` ile değil, dosyaya yazılıp `psql -f` ile koşulur. **İki blok KALICI operatör scripti oldu (dersler içine işli, çağıran yok, salt manuel): `scripts/ops-backup-prod.ps1` (yedek al, SHA/TOC bas) + `scripts/ops-restore-drill.ps1 -ExpectedSha <SHA>` (prova; migration beklentisini repodaki `prisma/migrations` klasör sayısından OTOMATİK türetir → gelecekte bayatlamaz; sentinel==toplam yapısal değişmezi; koşum: LixusPreflight klasöründen `powershell -NoProfile -ExecutionPolicy Bypass -File ...`; ⚠️ **`-ExpectCalendarSources 8` DE VER** — bu bayrak olmadan `sentinel==toplam` değişmezi BOŞ tabloda trivially sağlanır [0==0] ve takvim verisi tamamen kaybolmuş bir yedek provanın her adımından yeşil geçer; script artık 0 satırda uyarıyor ama gerçek pin bu bayrak). Aylık ritüel: klonu GÜVENLİ güncelle (`git status --short` → çıktı varsa DUR; `git fetch` + `git pull --ff-only` — operatör klonunda ASLA `reset --hard`, yerel değişiklik siler; reset yalnız Claude konteyner-kurtarmasının aracıdır) → backup scripti → çıkan SHA ile drill scripti. ⚠️ **Bu scriptler OTOMATİK yedek/PITR DEĞİLDİR** (Codex netleştirmesi) — hedef mimari ÜÇ katman: (1) Railway PITR (dakika hassasiyetli geri dönüş; kaynak DB'yi değiştirmeden seçilen andan yeni Postgres servisi çıkarabilir; ⚠️ etkinleştirme Postgres'i yeniden deploy EDEBİLİR → düşük trafikli saatte yap; ön şart olan taze doğrulanmış dump BUGÜN elde = hazır), (2) Railway zamanlanmış yedekler (günlük/haftalık), (3) bu iki script = Railway-dışı doğrulanmış arşiv + aylık gerçek prova. 1 ve 2 panel işi, KULLANICI yapar.**
